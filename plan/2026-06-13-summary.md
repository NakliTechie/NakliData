# Day summary — 2026-06-13 (session 4 — v1.4.0 release → Chunk 3 backlog → slice-B validation + fix)

Marathon session, all on `main`, all CI-green + deployed. Resumed via
`/resume`, ran the v1.4.0 release, cleared the Chunk 3 backlog, then the
user greenlit running the owed W3.2 slice-B validation in a real browser
(Chrome MCP) — which found real bugs, fixed two, and grounded the third.

## Shipped (10 commits, `1d45510..78fce01`, all pushed + CI-green)

- `1d45510` — **v1.4.0 tagged + released** (annotated tag `v1.4.0`). Notes
  `plan/v1.4.0-release-notes.md`; README v1.4 surfaces; DECISIONS AP.
- `df54216` — **Batch A** parked forward-pass minors (query-builder M17,
  lineage-store M19, refresh L1, +L2/L3/L4/L5/L15).
- `85d34af` — **Batch B** (notebook **M14 runAll topological order**, M9
  Enter-to-add, M10 `confirmModal`, L12 dispose cohort/assertion editors).
- `811d5cc` — **Batch C** stray cleanup (S1 dead chart-config validators,
  L14; ticked S2/S11 already-resolved).
- `3c06ba0` — **F5 multi-column window partitions** (calc-field; core was
  already array-capable, UI exposed it).
- `9a984a5` — **F6 multi-step query-builder pipelines** (derived steps;
  `emitPipeline` nests base as `FROM (…) AS step_N`, injection-safe).
- `3f725b9` — Chunk 3 docs (STATUS + DECISIONS AQ/AR/AS + pending + workplan).
- `0a5b98e` — slice-B validation **FAIL** writeup (DECISIONS AT).
- `9703a5f` — **local-model path fix** (WebGPU device + q4f16; split-singleton
  registration; graceful OOM; labels; 0.5B default; DECISIONS AU).
- `78fce01` — **cloud-BYOK smoke coverage** + Layer-3 R&D note.

## Verified

- **Gates green throughout:** 819 vitest (was 798) · `npm run check` ·
  `npm run smoke` (now incl. a cloud-BYOK step) · 55 e2e in CI · bundle
  **677.7 KB / 750 KB**. CI verify gate green on every push.
- **F5 + F6 Chrome-verified** via throwaway Playwright runs (removed).
- **Slice-B re-validation done live** in real Chrome + WebGPU (Chrome MCP):
  models load (0.5B + 1.5B q4f16), generator registers, jobs dispatch.
- **Cloud-BYOK path:** wiring now smoke-verified (mocked transport). **Still
  owed:** a live real-key call (manual, BYOK — can't be in CI).
- **Still owed:** Layer-3 local-inference output quality (see Open questions).

## Decisions

- **AP** — cut v1.4.0 from the green slate; no new amendments; e2e not re-run
  (docs-only release).
- **AQ** — fixed the actionable parked minors; logged won't-fix rationale for
  M11/S3, L18, L24, S12, S16, + cosmetic L/S items.
- **AR/AS** — F5 UI-only (core already multi-column); F6 nests subqueries
  reusing the injection-safe emitter; `emitSql` gained `omitLimit`.
- **AT** — slice-B FAIL: local model OOM'd on wasm (`bad_alloc`) AND plain-q4
  WebGPU; size labels ~2× understated; load froze the tab ~45 s.
- **AU** — fixes: WebGPU + **q4f16** (the 1.5B fits at 1.14 GB); **split-
  singleton registration** (chunk returns generator, main bundle registers —
  same class as AJ); graceful OOM; **Qwen2.5-0.5B fits-anywhere default**;
  local provider labelled **experimental**.
- **nl2sql** (conversational): NOT adopting GoogleCloudPlatform/nl2sql —
  server-side + Google/Vertex-coupled, violates no-server + BYOK. Keep the
  A23 design; borrow few-shot + dry-run-EXPLAIN self-correction later.
- **Cloud-BYOK honesty:** corrected an overclaim — I'd asserted "cloud works"
  from the shipped-default + eval harness (recorded fixtures, not live).
  Added a real smoke step instead of leaving it unverified.

## Tried then rolled back

- **Plain q4 on WebGPU for the 1-2B models** — still OOM'd (GPU). Switched to
  `q4f16` (fp16 activations ≈ half the GPU working set) → the 1.5B fits.
- **Greedy → light sampling** (`temperature 0.3, repetition_penalty 1.2`) to
  fix the degenerate local output — changed the *flavour* of garbage
  (`{SQL!!!!!!` → `'\'%-*02*'`) but did not rescue it. Kept the sampling (a
  reasonable anti-degeneracy default); the real cause is deeper.

## Open questions

- **Layer-3: why does in-browser local inference produce garbage?**
  (`{SQL!!!!!!` / `'\'%-*02*'`, near-random, consistent across jobs + model
  sizes.) Top hypothesis: onnxruntime-web WebGPU q4/q4f16 kernel correctness;
  then chat-template mismatch. Grounded plan in
  `plan/w32-local-inference-rnd.md`. Deferred — "take it up later."
- **NL→SQL quality** for weak models — optional few-shot DuckDB examples +
  dry-run-EXPLAIN self-correction (constraint-safe). Not started.
- **Playwright cache eviction** mid-session forced `npx playwright install
  chromium` to re-run smoke locally — watch for recurrence.
