## Last update: 2026-07-05T16:30:00Z — **Polyglot-Workbench Fork 2 COMPLETE: the R cell SHIPPED (DECISIONS CH + A35).** Fork 2's second language. A new `r` cell mirrors the Python cell: upstream result → R over a data.frame `df` → re-registered as `cell_<id>`. **Runtime:** WebR 0.2.0 **vendored same-origin** (~66 MB incl. the R VFS; copied from the pinned `@r-wasm/webr` devDep by `scripts/fetch-webr.mjs`, gitignored), loaded by the `webr-runtime` lazy chunk. **Needs SharedArrayBuffer** → the COOP/COEP isolation enabled in CG (the reason we turned it on); also must load same-origin (CDN build threw `ASM_CONSTS` cross-origin). **Interchange: CSV over WebR's VFS + base R** (`read.csv`/`write.csv` — no R package, no cross-origin install; sovereign; types inferred both ways, dates→strings the tradeoff). New engine helpers `queryToCsvBuffer`/`registerCsvBuffer`. **Shared language cell:** `python-cell.ts` → `language-cell.ts` (one `renderLanguageCell` for python+r; `handleRunLanguage` dispatches to the right runtime chunk) — forced by the shell budget + the right design. **A35:** bundle cap 750→**768 KB** (a second language cost ~1 KB eager; compute stays in lazy chunks). **Run state:** **956 vitest** · `npm run smoke` green **+1 leg** (SQL → CSV → WebR base R → CSV → DuckDB, downstream sum=120; Python leg still passes) · `npm run check` clean · bundle **750.8/768**. Live-verified: WebR inits on the SAB channel + a real `aggregate()` round-trips. **Fork 2 done (Python + R).** Server-run kernels stay out (commercial). **Next:** a v1.7.0 release (R cell + COOP/COEP since v1.6.0), or the parked roadmap.

## Earlier last-update: 2026-07-05T14:30:00Z — **v1.6.0 RELEASED + tagged.** Cut from the green slate at `44c3d07`. **Tag `v1.6.0`** on `origin/main`; GitHub release live (https://github.com/NakliTechie/NakliData/releases/tag/v1.6.0); Cloudflare Workers Build auto-deploys on push. The v1.5.2→v1.6.0 delta: the **Python cell** (Polyglot Fork 2), the **Facet view track** (Embedding · Network · Knowledge-graph · Weighted · Temporal · Distribution), **SPSS/Stata/SAS** (ReadStat-wasm), **offline Parquet + Spatial**, **Arrow**, resilient **SQLite (sql.js) + CSV**, and onboarding (help modal · welcome splash · field guide). All additive; old `.naklidata` files round-trip; versioning stays git-tag-based (`BUILD_VERSION` `0.1.0`). Canonical changelog: `docs/release-notes/v1.6.0-release-notes.md`. **No new spec amendments** (Facet + Python cells extend the A34 lazy/budget-exempt envelope). **Next:** R cell (WebR) on the Fork-2 rails.

## Earlier last-update: 2026-07-05T14:00:00Z — **Polyglot-Workbench Fork 2: the Python cell SHIPPED (DECISIONS CF).** A new lazy, opt-in **`python` cell**: takes an upstream result cell, runs the user's pandas code over a DataFrame `df`, and re-registers the result as `cell_<id>` — queryable downstream. **Sovereign runtime:** Pyodide 0.27.7 + pandas + pyarrow **vendored same-origin** (`scripts/fetch-pyodide.mjs` → `public/pyodide/`, ~33 MB, bytes gitignored / integrity.json pinned; postinstall). Loaded by the `pyodide-runtime` lazy chunk (nothing eager but the render). **CSP:** runs pandas+pyarrow with **zero violations** under `script-src 'self' 'wasm-unsafe-eval'` (no `unsafe-eval` — Python compiles in the wasm runtime). **Interchange: Parquet** (DuckDB `COPY … TO (FORMAT parquet)` + `copyFileToBuffer` out / `read_parquet` back) — no apache-arrow on the JS side, sidesteps the Arrow-instance identity problem; pyarrow reads/writes Parquet in Python. **UX:** "Downloading Python (~33 MB)…" affordance, pre-warmed imports, head-snapshot preview, input capped at 2 M rows (refuse-not-OOM). Pure `.naklidata` config (preview dropped on save; old files round-trip). **Latent bug fixed:** a code textarea persisting on blur re-rendered the notebook and **dropped the Run click** (detached button) → added `Notebook.patchCellSilent` + `CellHandlers.onChangeSilent`. **Run state:** **956 vitest** · `npm run smoke` green **+1 leg** (SQL → Parquet → Pyodide(pandas) → Parquet → DuckDB table, downstream `sum=120`) · `npm run check` clean · bundle **749.8/750** (compute in the chunk; render+wiring trimmed hard to fit). Live-verified under the real CSP. **Next (per the vision):** R cell (WebR) on the same rails; a richer editor later. Server-run kernels stay out (commercial).

## Earlier last-update: 2026-07-05T11:30:00Z — **Polyglot-Workbench Fork 2 round-trip spike PASSED — Python cell greenlit (DECISIONS CE).** Fork 2's blocking gate was a spike: prove `DuckDB → Arrow → pandas → compute → Arrow → DuckDB` is fast + memory-sane with two wasm heaps live on ≥1M rows before building any shell. Ran it as a throwaway (`eval/spikes/fork2-roundtrip/`, **no product code**): DuckDB-wasm 1.29 + apache-arrow 17 + **Pyodide 0.27.7** (pandas + pyarrow), one tab, full round-trip with integrity checks. **PASS:** 1M rows round-trips in **499 ms** (~640 MB combined heaps; DuckDB↔Arrow hops ~45 ms each, rest is pandas), no OOM; survives 5M (3.2 s, 1.3 GB Pyodide heap). Named escalation (reframe Python as export-only) does NOT trigger. **Build constraints the spike surfaced:** (1) **pin Pyodide 0.27.7** — `pyarrow` ships ONLY in 0.27.x, not 0.26/0.28 (load-bearing); (2) **vendor ~30 MB same-origin** (DuckDB+Pyodide+pandas+pyarrow+numpy) + OPFS/HTTP cache so cold load (~4 s w/ download) is one-time, warm ~2.3 s, behind a "Downloading Python…" affordance; (3) pre-warm the first import (~1.2 s JIT); (4) cap rows into a Python cell (~a few million; ~300 MB Pyodide heap/1M rows). Full numbers: `eval/spikes/fork2-roundtrip/FINDINGS.md`. **Sovereign holds:** runtime *code* fetched + surfaced, *data* never leaves the tab. **No gates run** (spike-only, product untouched — check/test/smoke unaffected from the Fork-1 state: 956 vitest · smoke green · bundle 744.6/750). **Next:** build the lazy opt-in `python` cell (`Arrow in → Arrow out`, result re-registers as a DuckDB table; A34 budget-exempt) per the vision doc; R (WebR) follows.

## Earlier last-update: 2026-07-05T10:00:00Z — **SPSS/Stata/SAS mounting restored via a vendored ReadStat-wasm reader (DECISIONS CD — Polyglot-Workbench Fork 1).** F3 dropped the stat formats because DuckDB's `read_stat` has no wasm build; Fork 1 reopens that by **owning the reader** (same posture as the sql.js/SheetJS bypasses). Compiled **ReadStat** (WizardMac @ `3c68974`, emcc 6.0.1, read-side only + zlib for `.zsav`) to a same-origin wasm via a ~90-line C wrapper that emits NDJSON. New lazy chunk `src/lazy/readstat-reader.ts` → `Engine.registerReadStat` → `read_json_auto` (single table/file; `.por` re-derived from the extension). **Artifacts committed** (`public/readstat-wasm/readstat.wasm` 275 KB + `src/vendor/readstat/readstat-glue.js` 62 KB + `rs_wrapper.c`/`build.sh`/README with the pinned commit) — no npm/CDN source and CI has no emcc, so the prebuilt is the only sovereign option; biome ignores the glue. Formats re-added to detectFormat + union + router + both pickers + the supported-types message. **Sovereign** (wasm same-origin, data never leaves the tab), budget-exempt lazy chunk. **Verified:** real `auto.dta` → **74 rows** offline through the real add-source UI; `.dta`/`.sav`/`.xpt` parse in Node (auto.dta 74×12, correct types + nulls). **Run state:** **956 vitest** (mount.test routes stat→registerReadStat again) · `npm run smoke` green **+1 leg** (committed 3-row `.dta` fixture → 3 rows) · `npm run check` clean · bundle **744.6/750**. **Known limit:** SPSS/Stata dates come through as raw numeric offsets (v1; format-string decoding is a future refinement). **Fork 2 (Python/R compute cells)** stays gated behind the round-trip spike (`plan/polyglot-workbench-vision.md`). **Next:** back to the parked roadmap, or the Fork-2 spike if prioritised.

## Earlier last-update: 2026-07-05T08:15:00Z — **F4 + F5 done → BX cross-format queue CLOSED (DECISIONS CB + CC).** **F4 (headerless CSV):** `createDelimitedView` forced `header=true`, so a headerless file's first data row became the column names (one record lost). Dropped the explicit `header=` → DuckDB's sniffer auto-detects. Verified live (offline, real add-source): headerless *typed* CSV → 3 rows + `column0/1/2` (was 2 rows + data-as-headers); headered CSV → correct headers, no regression; real `recent-grads.csv` (173) + `ag-exports.csv` (50) mount clean. **Documented limit:** all-VARCHAR headerless (e.g. `addresses.csv`) stays inherently ambiguous — DuckDB decides; a "first row is data" toggle was considered and not built (narrow case, adds UI/persistence surface). **F5 (picker):** mode-gating is moot after F1–F3 — every advertised format now works in BOTH modes (parquet/spatial vendored offline; xlsx/sqlite/arrow are vendored lazy chunks; stat formats dropped). Kept the useful part: the unsupported-mount throw now names the supported set ("NakliData mounts CSV, TSV, JSONL/NDJSON, Parquet, Arrow/Feather, Excel, SQLite, DuckDB, GeoJSON/KML") instead of a bare "unsupported extension". **Run state:** **955 vitest** · `npm run smoke` green **+1 leg** (headerless typed CSV → 3 rows) · `npm run check` clean (exit 0) · bundle **743.4/750**. **BX queue F1–F5 fully closed** — final coverage: CSV·TSV·JSONL(flat+nested)·Parquet(offline+online)·Arrow/Feather(uncompressed)·Excel·SQLite·DuckDB·GeoJSON/KML, all in both modes; stat formats intentionally dropped. **Next:** back to the parked roadmap (Facet crossfilter propagation · later Facet views · Resolve deepening) — see `plan/pending.md`.

## Earlier last-update: 2026-07-05T07:20:00Z — **F2 + F3 done (DECISIONS BZ + CA — BX queue continues).** **F2 (Arrow):** `.arrow`/`.feather` are Arrow IPC *file* format (`ARROW1` magic), but `registerArrow` fed them to `insertArrowFromIPCStream` (which wants IPC *stream*) → silent no-op → "table does not exist" (broken online + offline). Fix: new lazy chunk `src/lazy/arrow-reader.ts` — `arrowToStreamIPC()` re-frames file→stream via apache-arrow's `tableFromIPC`/`tableToIPC` (only Uint8Arrays cross the boundary). Promoted `apache-arrow@^17` (duckdb-wasm's own version) to a **direct dep**, imported **only** in the 196 KB lazy chunk → shell bundle untouched. **Known limit:** LZ4/ZSTD-compressed Arrow can't be read (apache-arrow JS has no IPC decompression) — the real test file was LZ4; the chunk throws an actionable "re-export uncompressed / use Parquet" error. **F3 (stat formats):** probed the real cause — `read_stat` is **not published for wasm at all** (404 across every version/wasm target; `h3` returns 200 for the same path, so the probe is valid), so the `allow_unsigned_extensions` config-time flag was a red herring — there's no wasm binary to load. **Dropped** `.sav/.zsav/.por/.dta/.sas7bdat/.xpt` from `detectFormat`, the `FileFormat` union, the mount router, both picker accept lists, and removed `Engine.registerReadStat` — they now surface the normal "Unsupported file extension" path instead of advertising a dead mount. **Run state:** **955 vitest** (+3 arrow-reader; mount.test now asserts stat→null + refused) · `npm run smoke` green **+1 leg** (674-byte uncompressed feather → 3 rows via real add-source) · `npm run check` clean (exit 0) · bundle **743.8/750**. **Next in queue:** F4 (headerless CSV) + F5 (mode-aware picker gating) — the last BX items.

## Earlier last-update: 2026-07-05T06:10:00Z — **F1 SHIPPED: Parquet + Spatial work offline (DECISIONS BY — closes BX's headline gap).** Both `read_parquet` and spatial's `ST_Read` **autoload** their DuckDB extension from the extension repo, not the wasm bundle; an offline boot pins `custom_extension_repository` local (engine.ts:280), so the autoload 404'd and both formats were **dead in the shipped/offline experience** (app boots `offline:true` by default + on both deploys). **Fix:** added `parquet` + `spatial` to `EXTENSIONS` in `scripts/fetch-duckdb-extensions.mjs` (both exist at extensions.duckdb.org for `v1.1.1/wasm_eh`). Postinstall vendors the bytes into `public/duckdb-extensions/v1.1.1/wasm_eh/` (gitignored; only `integrity.json` committed). Pin regenerated by bootstrap (deleted + re-fetched) — verified existing `json`/`sqlite` hashes are byte-identical → clean widening, not a supply-chain swap. **Sizes:** parquet 2.7 MB, spatial 22 MB — both served as **separate lazy assets** under `duckdb-extensions/`, fetched only on first Parquet/GeoJSON mount; **neither touches the `dist/index.html` bundle** (stays 743.8/750). The 22 MB spatial payload is an accepted one-time offline-GeoJSON download. **Verified offline (`?offline=1`, real UI Add-source path):** `green_taxi.parquet` → **56,551 rows** (264 ms) · `us-states.geojson` → **52 features**. **Run state:** `npm run smoke` green **+2 legs** (committed regression guard: an 822-byte inlined 5-row Parquet + a 2-feature GeoJSON, both mounted through the real add-source modal offline) · **951 vitest** · `npm run check` clean (exit 0; the 24 biome warnings are pre-existing, none in my files) · bundle **743.8 KB / 750 KB** (6.2 KB headroom). **Next in the BX queue:** F2 (Arrow IPC-file reader), F3 (`read_stat` config-time flag or drop stat formats), F4 (headerless CSV), F5 (picker gating).

## Earlier last-update: 2026-07-04T20:30:00Z — **Cross-format + dirty-file audit — findings LOGGED, fixes deferred (DECISIONS BX).** Followed the SQLite/CSV/XLSX test with a real-data sweep across every advertised format (NYC green-taxi parquet, Stata .dta, US-states GeoJSON, GitHub-commits nested JSONL, real messy CSVs; Arrow/TSV transcoded from real taxi data — Kaggle unavailable, no creds). Driven live via Chrome MCP against the real engine (temp `?debug` hook, since removed). **Working: CSV · TSV · JSONL (flat + nested→STRUCT, classifier-safe) · XLSX · SQLite.** **Broken: (a) Parquet — ❌ offline / ✅ `?cdn=1` — not vendored + offline pins the extension repo local; since the app boots offline by default AND on both deploys, parquet is dead in the shipped experience (the headline; `.assetsignore` skips only duckdb-fallback/, not duckdb-extensions/, so vendoring fixes it everywhere). (b) Spatial (GeoJSON/KML) — same offline gap. (c) Arrow/.feather — ❌ everywhere — registerArrow uses insertArrowFromIPCStream (stream) but files are IPC file format (ARROW1). (d) Stata/SPSS/SAS (read_stat) — ❌ everywhere — community-ext loader does runtime `SET allow_unsigned_extensions` which DuckDB-wasm rejects (config-time only).** **Dirty data:** headerless CSV mis-headered (first data row → column names; createDelimitedView forces header=true); everything else degraded well (nested JSON→STRUCT no-crash, R rownames/space-names/NA nulls typed fine, messy-CSV skip tally held). **No code fixes applied this pass** — the SQLite/CSV/add-source/introspection fixes (BW) remain the last shipped change. **Queued: F1** vendor parquet+spatial · **F2** Arrow IPC-file reader (apache-arrow tableFromIPC) · **F3** allow_unsigned_extensions at boot-config (or drop stat formats) · **F4** headerless-CSV handling · **F5** gate picker + smoke + docs. **Working tree:** debug hook removed; DECISIONS BX + this entry are the durable log. **Next:** F1–F5 when work resumes.

## Earlier last-update: 2026-07-04T19:15:00Z — **Real-data test fixes: SQLite mount unbroken via sql.js, "+ Add source" wired, resilient CSV, introspection passthrough (DECISIONS BW).** An end-to-end junior-analyst test (mount public CSV/XLSX/SQLite, build reports) found four issues; all four fixed + verified. **#1 SQLite was fully broken** — DuckDB-wasm's `sqlite_scanner` VFS can't open a browser-*registered* file (ATTACH "succeeds", first read throws `unable to open database file`). Proven exhaustively via a live Chrome REPL: fails for **every** registration protocol (buffer / FILEREADER dio-off+on / OPFS-FSACCESS) **and** across DuckDB cores **v1.1.1 + v1.3.2** — architectural, not a version bug (a `read_blob` on the same file returns all bytes, so it's not seekability). **Fix:** new lazy chunk **`src/lazy/sqlite-reader.ts`** reads the file with **sql.js** (SQLite-wasm), extracts each table → NDJSON → DuckDB `read_json_auto` (per-column type inference; SQLite date-text → TIMESTAMP). New dep **`sql.js`** (Apache-2), lazy + **vendored same-origin** (`public/sqlite-wasm/sql-wasm.wasm` ~640 KB via `scripts/vendor-sql-wasm.mjs`, sovereign), emscripten Node-`require`s stubbed by an esbuild plugin. Chinook's 11 tables mount in ~210 ms; 4-table joins + revenue reports run. **#2** "+ Add source" (was an unwired stub) now opens a mount-options modal (`shell.ts:mountOptionsHtml()` shared with the empty state). **#3** messy CSVs no longer hard-fail — `createDelimitedView` uses full-file inference + `ignore_errors`/`null_padding` and surfaces a non-fatal "Loaded N; skipped M" notice. **#4** `SHOW`/`DESCRIBE`/`PRAGMA`/`EXPLAIN`/`SUMMARIZE` now run directly instead of being wrapped in `CREATE VIEW` (fixed the baffling "syntax error at or near SHOW"). **Run state:** **951 vitest** · `npm run smoke` green (+2 legs: SQLite mount via Add-source modal → 2 tables classified; `SHOW TABLES` runs direct) · `npm run check` clean · bundle **743.8 KB / 750 KB** (6.2 KB headroom; sql.js glue is an off-budget lazy chunk). Live-verified in Chrome. **Guide (`/guide-nt`) additions:** a new **"Reports from real datasets"** section — three board-ready cuts captured live (Superstore CSV profit/margin by region · MS Financial Sample XLSX by segment · Chinook SQLite revenue by country), datasets committed under `guide/sample-data/` (superstore trimmed to 3k rows); plus a **lightbox** on every screenshot (click to zoom, `←/→` + `↑/↓` to step through visible shots, Esc to close). Generator-driven (`guide/{capture,build}.mjs`), regenerated → `guide/index.html` (32 cards / 9 sections). **Next:** the parked roadmap views / crossfilter propagation.

## Earlier last-update: 2026-07-04T18:30:00Z — **Three more Facet views shipped: attributed edges (Knowledge-graph + Weighted), Temporal, Distribution (DECISIONS BV).** Built the next roadmap batch, each a committed + gated increment. **Attributed edges** extend the Network cell (not new kinds): an `edgeColorCol` colours edges categorically + a click-to-filter legend (typed/KG view); an `edgeWidthCol` scales line width (Weighted view). The categorical palette moved to **`core/categorical-palette.ts`** (single source; the cell's legend swatches now match the deck chunk's colours exactly). **Temporal** (new `temporal` cell) — `core/temporal.ts` (coerceTime + bucketTime + countInWindow) → an SVG bar timeline with a **brushable window** reporting the range + in-window row count. **Distribution/Categorical** (new `distribution` cell) — `core/distribution.ts` auto-classifies a column (numeric→histogram, else top-N category bars); **click a bar to select** it + report its row share. **Two scope calls (BV):** (1) crossfilter propagation of the temporal window + distribution selection to downstream cells is **deferred** (needs the selection-store machinery — shared follow-up, not per-view v1); the views are visual+readout for now. (2) The chart-style SVG cells **lazy-load** — eager they pushed the bundle to 747/750 (3 KB), so their render bodies moved to a shared **`src/lazy/facet-charts.ts`** chunk (chrome stays in main) → 740.1/750. **Run state:** **951 vitest** (+23: palette 5 · temporal 9 · distribution 9) · `npm run smoke` green (+3 legs) · `npm run check` clean · bundle **740.1 KB / 750 KB** (9.9 KB headroom). All pushed to `main`. **Facet views now live:** Embedding · Network · Knowledge-graph (typed edges) · Weighted · Temporal · Distribution/Categorical. **Next:** remaining roadmap views (Hierarchy/tree · Geospatial — Map cell already covers the latter), the deferred crossfilter propagation, or wind down.

## Earlier last-update: 2026-07-04T17:30:00Z — **deck.gl deduped into ONE shared `deckgl.ts` chunk (DECISIONS BU — closes BT's owed follow-up).** The embedding, network, and map-overlay renderers used to be three separate lazy chunks (`deckgl-embedding` / `deckgl-network` / `deckgl-points`), each bundling its own full copy of **deck.gl + luma.gl** (~600 KB duplicated per chunk on disk); when two loaded in a session luma.gl logged a benign "This version of luma.gl has already been initialized". **Now: one self-contained `src/lazy/deckgl.ts`** hosts all three behind separate exports (`mountEmbeddingScatter` / `mountNetworkGraph` / `mountDeckGlPoints`) + the shared categorical palette → deck.gl bundled **once** (641 KB chunk), luma inits **once** (every cell imports the same `./chunks/deckgl.js` URL → one cached module), warning gone. **Rejected the tempting route** (a shared `deckgl-core.ts` + esbuild `splitting:true` across the deck family): it was correct on disk but **corrupted GPU picking non-deterministically** — code-splitting reordered deck.gl/luma.gl's *circular* module graph, so find-similar picked nothing one run and network `pickObject` asserted the next (the un-deduped original passed the same legs — the split introduced it). Single self-contained chunk keeps init order identical to a normal bundle. `esbuild.config.mjs` stays `splitting:false`; `lazy-loader.ts` registry drops the three entries for one `deckgl`; the three cells now `loadChunk('deckgl')`. **Run state:** **928 vitest** · `npm run smoke` green ×2 (both facet legs — find-similar + find-neighbours — pass; **luma "already initialized" warning confirmed gone, 0 occurrences**) · `npm run check` clean · bundle **732.0/750** (shell unchanged — deckgl is a budget-exempt A34 lazy chunk). **Next:** later Facet views (Temporal · Distribution · Categorical · Hierarchy · Geospatial), or the parked non-Facet items.

## Earlier last-update: 2026-07-04T16:00:00Z — **Facet Network view SHIPPED (DECISIONS BT — workplan Chunk 2 done).** The force-graph view is live: a new **`network` cell kind** takes a SQL edge list (source-id + target-id columns), builds distinct nodes (degree-sized), lays them out, and draws them with deck.gl (LineLayer edges + ScatterplotLayer nodes); click a node → its immediate neighbourhood highlights (rest dims), background click clears. **Two live-verify surprises corrected the de-risk (BS→BT):** (1) `@antv/layout-gpu` (the spiked GPU path) **trips the CSP** — its GPGPU backend compiles kernels with `new Function`, blocked by our `script-src` (has `wasm-unsafe-eval`, not `unsafe-eval`); the spike missed this on a CSP-less page. (2) `@antv/layout` v2 pure-JS force is **`requestAnimationFrame`-driven** → stalls in a backgrounded tab (the PCA/setTimeout footgun again, BR). `-wasm` needs SAB/COOP-COEP (rejected), d3-force is a Hard NOT (§10). **Resolution:** own the layout like PCA — **`src/core/force-layout.ts`**, an in-house synchronous Fruchterman (seeded/deterministic, CSP-clean, no dep, no rAF, elapsed-time yields, 8 unit tests incl. community separation). The **`deckgl-network` lazy chunk is render-only** (deck.gl; layout runs in core before the chunk loads). Honest ceiling **`NETWORK_LAYOUT_MAX = 3000`** (O(n²) sync) — above it the cell says "filter down / precompute x,y + Embedding cell" rather than freezing; WebGPU-compute force is the future scale path. `@antv/layout-gpu` added then removed. **Run state:** **928 vitest** (+7) · `npm run smoke` green (+1 leg: real SQL edge list → layout under the real CSP → deck.gl canvas → find-neighbours → clear — this leg *is* the CSP-regression guard) · `npm run check` clean · bundle **732.0/750** (18.0 KB headroom). Live-verified in Chrome. Known follow-up: embedding + network chunks each bundle deck.gl (benign luma.gl "already initialized" when both load) → dedupe into a shared deck chunk later. **Next:** later Facet views (Temporal · Distribution · Categorical · Hierarchy · Geospatial), or the shared-deck-chunk dedupe, or the parked non-Facet items.

## Earlier last-update: 2026-07-04T14:30:00Z — **Facet Network view DE-RISKED: GPU force layout resolves the COOP/COEP blocker by avoidance (DECISIONS BS — workplan Chunk 2).** The Network view was blocked (BM) on the layout engine at scale: pure-JS `@antv/layout` is unusably slow (7–26 s @ 2.6k) and the assumed accel path `@antv/layout-wasm` needs `SharedArrayBuffer` → cross-origin isolation (COOP/COEP) → collides with the cross-origin DuckDB CDN load + OSM tiles + HF fetches. **Answer: `@antv/layout-gpu` (WebGL float-texture GPGPU) — no SAB, no COOP/COEP, no header changes**, so the whole isolation problem is *sidestepped, not solved*. De-risked live (round-2 spike, `eval/spikes/gpu-layout-spike.*` + FINDINGS): capability probe confirms `crossOriginIsolated:false`/`SAB:absent` (so `-wasm` is DOA today) but `WebGL2`+`EXT_color_buffer_float`+`OES_texture_float_linear` all present (GPU path ready). Ladder (clustered graphs, avgDeg 4, 200 iters, M4 Pro): **Fruchterman GPU** (validated default, resolves cleanly) 10k/1.2s · 50k/7.5s · 100k/26s (O(n²)); **GForce GPU** sub-quadratic 100k/**5s** but needs seeded initial positions + a cluster-separation quality check before it's the default. **Refines BF:** honest in-browser force ceiling is ~10k interactive / ~50k compute-once-and-cache / 100k background — NOT "routine 1M" (deck.gl *render* at 1M still stands; the *layout* is what's capped). **Run state:** no product-code change this session (spike + docs only) — gates unchanged from 12:00Z (**921 vitest · smoke green · check clean · bundle 724.4/750**). **Next:** scaffold the Network view cell (mirror `embedding-cell.ts`: source/target SQL cell → `@antv/layout-gpu` in a lazy chunk → deck.gl LineLayer+Scatter, cache computed x/y like Embedding caches precomputed x/y). `@antv/layout-gpu@1.1.7` becomes a real lazy-chunk dep (budget-exempt, A34) at scaffold time.

## Earlier last-update: 2026-07-04T12:00:00Z — **Facet Embedding view made INTERACTIVE: click-to-find-similar + in-browser PCA projection (DECISIONS BR — workplan Chunk 1, the keystone).** New **`emb` picker** (`embCol` on `EmbeddingCellState`; old `.naklidata` files round-trip — the key reads `?? null`): clicking a point ranks the result's own precomputed vectors with `rankBySimilarity` (core/embed-search JS path — **no model download, no engine round-trip**) and highlights the top-10 (selection ringed + enlarged, neighbours full colour, rest dimmed to α40; background click clears; tip pins the neighbour summary). With `emb` set but **no x/y**, the cell **auto-projects to 2-D with PCA**: new **`src/core/project2d.ts`** — `coerceVector()` (Arrow list values arrive as typed arrays / JS arrays / vector-likes / JSON strings; all handled) + `pcaProject2D()` (power iteration + deflation, deterministic init/sign, Float64 accumulation; **yields only after ~32 ms of compute** — per-iteration `setTimeout(0)` yielding was rolled back after a live repro: hidden tabs throttle timers to ≥1 s, hanging even a 40-vector projection). deck.gl chunk gained `onClick` + `setHighlight` + **`simulateClick(x,y)`** (real `deck.pickObject` GPU picking → same callback; stashed on the mount as `__embedScatter`) because synthetic PointerEvents can't reach deck's input manager — the smoke drives find-similar through it; future agent verbs can too. **Run state:** **921 vitest** (+15: coerceVector + PCA recovery/determinism/orthogonality) · **smoke green +2 legs** (real DuckDB `DOUBLE[]` → coerce → PCA → canvas headless; find-similar pick → tip pins → background clears) · `npm run check` clean · bundle **724.4/750** (+5.2 KB; 25.6 KB headroom) · **live-verified in Chrome @1440px** (scatter, pickers, highlight/dim/ring state, real background-click clear). **Next:** 2nd half of Chunk 1 is DONE with this; Network view is the next Facet surface (still gated on `@antv/layout` accel in-browser + COOP/COEP vs the DuckDB CDN); loose ends: A34/vision-doc BYOK ratification, intern's Brave + SQLite-copy re-tests.

## Earlier last-update: 2026-07-03T20:30:00Z — **SQLite mount field reports (FR-3): honest folder errors + read-failure guidance + `.db3` (DECISIONS BQ).** Intern hit "The requested file could not be read…" (single `.db`) and "No supported files found in 'dbmir'" (folder of SQLite files). Diagnosed both as the **same read failure** — Chrome's `NotReadableError`, which fires when a picked file mutates/locks after pick (textbook: a **live SQLite DB**). The folder scan was masking it as "unsupported." Fix (`src/core/mount.ts` + `src/main.ts`): `describeReadFailure()` (exported, tested) → actionable text ("likely open in another program… mount a copy"); `mountFolder` now distinguishes **found-but-unreadable** (surfaces the real reason) from **nothing-detected** (+ a no-recursion hint naming skipped subfolders); `getFile()` moved inside the per-file try; added **`.db3`** to `detectFormat` + both picker accept lists. Can't fix a genuinely live/locked DB in-browser — the message now tells the user to mount a static copy. **Run state:** **906 vitest** (+5) · smoke green · check clean · bundle **719.2/750**. Deferred: SQLite-magic content-sniff for extensionless DBs. **Next:** the embedSearch "find similar" interaction on the Embedding cell; then the Network view (pending accel-layout validation).

## Earlier last-update: 2026-07-03T19:00:00Z — **Onboarding: in-app Help modal + first-run welcome splash, both linking the illustrated field guide.** Built the searchable field guide first (`guide/` — a committed generator: `capture.mjs` drives the built app in headless Chromium and screenshots every surface; `build.mjs` assembles a single-file `guide/index.html` from caption data with inline search; `regenerate.sh` orchestrates. 24 shots · 8 feature sections · 0 console errors, themed from the app's own tokens). Then wired two in-app entry points to it: **`src/ui/help-modal.ts`** — `openHelpModal()` (header **Help** button — six key surfaces + keyboard shortcuts + guide CTA) and `maybeOpenWelcomeSplash()` (first-run, once-per-browser via `localStorage['naklidata.welcomed']`, warm 3-step welcome + "Browse example data" CTA + guide link). Reuses the shared `.schema-graph-overlay`/`.btn` surface; token inline styles only; Escape/backdrop/close + focus restore. Splash gated on genuine first visit (`!restoredFromSnapshot && !lensParam && !present`) at the end of `boot()`. **Guide link is relative (`guide/index.html`)**; `scripts/stage-guide.mjs` mirrors `guide/` → `dist/guide/`. **Staging runs inside the build** (`esbuild.config.mjs` prod branch) so it ships wherever the app does — the deploy is **Cloudflare Workers Builds** (git-integration, auto-deploys on push to `main`, builds `dist/` fresh), which runs the build, **not** `npm run deploy`; a predeploy hook alone never fired there (the first deploy 404'd on `/guide/` — fixed by moving staging into the build). **Run state:** **901 vitest** · `npm run smoke` green (+2 assertions: splash + Help modal both link the guide) · `npm run check` green (engine-boundary 10+10) · bundle **719.2 KB / 750 KB** (+7.1 KB modal; 30.8 KB headroom). Live Chrome check confirmed both surfaces render on-brand + the guide link resolves (HTTP 200). DECISIONS **BP**. **Next:** the embedSearch "find similar" interaction on the Embedding cell; then the Network view (pending accel-layout validation).

## Earlier last-update: 2026-07-03T17:00:00Z — **Facet Embedding view SHIPPED as a real NakliData cell (first productionised Facet view).** Turned the semantic-map spike (BN) into product code: a new **`embedding` cell kind**. **`src/lazy/deckgl-embedding.ts`** — a standalone deck.gl `Deck` (`OrthographicView`) scatter for precomputed (x, y), categorical palette (mirrors deckgl-points), hover labels; a lazy chunk (deck.gl never touches the shell), registered in `lazy-loader.ts`. **`src/ui/cells/embedding-cell.ts`** + `EmbeddingCellState` — mirrors `map-cell.ts`: pick an upstream SQL cell + x / y / color / label columns → renders the scatter; pure-config state → **persists in `.naklidata` with no schema change** (old files round-trip). Wired into `notebook.ts` (add-button, `addCell` case, render dispatch) exactly like every other cell. **Verified end-to-end, not just typechecked:** smoke asserts button → `addCell` → picker chrome; a **live Chrome check drove the real chunk and rendered a crisp 4-cluster coloured scatter** — and caught a real bug headless can't (deck.gl v9 leaves an explicit canvas at 300×150 + CSS-stretches it; fixed by sizing `canvas.width/height` to the container, same as the Network spike). **Run state:** **901 vitest** · `npm run smoke` green (+ embedding assertion) · `npm run check` green (engine-boundary 10+10) · bundle **712.1 KB / 750 KB** (+4.2 KB cell UI; deck.gl stays in the 614 KB lazy chunk, off the single-file budget). DECISIONS **BO**. **No layout dependency** (precomputed x, y) → ships free of the @antv/layout scale/COOP-COEP risk (BM). **Next:** the embedSearch "find similar" interaction on the cell; then the Network view (pending accel-layout validation).

## Earlier last-update: 2026-07-03T16:15:00Z — **Facet Embedding-map view VALIDATED end-to-end (the cleaner first view).** Built the semantic-map spike on the real corpus: embedded **1,964** citation papers (title+abstract) with all-MiniLM-L6-v2 → UMAP-2D → deck.gl `ScatterplotLayer` coloured by topic, rendered in Chrome. **Embeddings work** — `@huggingface/transformers` feature-extraction runs cleanly (Node: 1,964 in 10 s; cosine sanity holds — resnet~retinopathy > resnet~gan). Same model as the in-browser path + a **single encoder forward pass** (not the autoregressive q4f16 decode that broke local *generation*, BJ), so the **sovereign embedding path is sound** (in-browser WebGPU-embed confirm low-risk-owed). **The map shows real structure** — every tagged topic (covid / face / brain-tumor / skin / super-res / hyperspectral / …) forms a tight, well-separated cluster; same-topic papers sit together — visual proof the embedding pipeline is correct. **No force layout needed** (precomputed x,y → deck.gl direct), so the Embedding view **sidesteps the @antv/layout scale/COOP-COEP risk (BM) entirely** and is the **cleanest first *real* view to integrate into NakliData** — plus it's the foundation `embedSearch` already sits on. **Direction:** productionise the Embedding view first (before Network). Next: wire it into NakliData as a view cell (deck.gl scatter over a result's x,y cols + embedSearch "find similar"). Spike throwaway (`eval/spikes/`, biome-ignored); no product code touched — `check` green, bundle **707.9 KB**. DECISIONS **BN**.

## Earlier last-update: 2026-07-03T15:30:00Z — **Facet Chunk 2 STARTED — engine spike: deck.gl render confirmed, `@antv/layout` scale caveat.** First view-type-track work: de-risked the pinned engine (DECISIONS BF) on the real M0 citation graph (2,600 nodes / 10,638 edges). **deck.gl render ✅** — a standalone `Deck` (OrthographicView) + `LineLayer` edges + `ScatterplotLayer` nodes rendered the whole graph cleanly in Chrome (node size by in-degree, citation hubs coloured red) — the **first visible Facet Network view**. **`@antv/layout` ✅ works but ⚠️ JS too slow** — valid coords for all 2,600 nodes, but the pure-JS main-thread path is 7–26 s at just 2,600 nodes → categorically unusable at the 100k–1M BF target. **The accel path is required + constrained:** `@antv/layout-wasm` is browser-only + needs Web Worker + **SharedArrayBuffer → COOP/COEP cross-origin isolation** (collides with the cross-origin DuckDB CDN load); `@antv/layout-gpu` needs WebGL. **Removed `@antv/layout-wasm`** (unvalidated in-browser); **kept `@antv/layout` 2.0.0`** (validated engine). **BF refined, not reversed** — render confirmed; the "1M force" claim now has a named risk (accel layout must be validated in-browser + reconciled with COOP/COEP). **Next de-risk:** in-browser `@antv/layout-wasm`/`-gpu` at 50k–1M + the COOP/COEP tension; fallbacks = worker/server precompute or a WebGPU compute force sim. **The Embedding view (precomputed x,y) needs no layout → unblocked regardless; a good first *real* view to build.** Spike is throwaway (`eval/spikes/`, biome-ignored); no product code touched — `check` green, bundle **707.9 KB**. DECISIONS **BM**. Writeup: `eval/spikes/FINDINGS.md`.

## Earlier last-update: 2026-07-03T14:30:00Z — **Facet M0 scorer FIXED (projection-aware) + re-scored.** Fixed the #1 harness issue from the live run: `eval/m0/scripts/score.py` no longer uses exact result-set matching (which penalized correct answers that select a different/extra projection). Correctness is now **intent-correct** via `equivalent()` — the wider result is projected (every ordered column-subset) down to the narrower's arity and compared as a numeric-tolerant **row multiset**, crediting BOTH over-selection (`SELECT pid, ttl, n_cite` vs ref `ttl, n_cite`) and under-selection (model returns just the id where ref had id + title), while still requiring the right rows in correlation + same row count. Exact-match kept as a reported secondary. **Re-scored the live run:** DeepSeek **50% → 77%**, Qwen-3B **25% → 31%**. Verified the residual DeepSeek misses are GENUINE (NULL-venue/lang handling, `LIMIT 100` where 10 was asked, over-broad affiliation match) — so 77% is honest, not inflated. **Gate outcome:** the DeepSeek **ceiling now clears T1=70%** (metric + tasks sound), but **no free rung does** (L1-3B 31%, L2 in-browser-WebGPU broken) → G1 still fails on the free rungs; still NOT the free-AI→BYOK-AI hard-stop (the free Ollama path scales with size + is untried at 7B+; WebGPU is a fixable numerical bug). G3 safety 0/6 leaks on every rung. Residual metric ambiguity → the owed G5 LLM reference-judge. DECISIONS **BK**. **Remaining levers:** dtype A/B (q8/fp16) to un-break the WebGPU rung; a 7B+ Ollama model for the free path; run the semantic half (G4). No product code touched — `check` green, bundle **707.9 KB**. Full writeup: `eval/m0/FINDINGS-2026-07-03.md`.

## Earlier last-update: 2026-07-03T14:00:00Z — **Facet M0 RAN LIVE — first real gate numbers.** Drove the harness end-to-end on this machine: **Ollama** (L1), **in-browser WebGPU/Transformers.js** (L2, in a real Chrome via the browser MCP), and **DeepSeek** (C1 ceiling; key injected server-side by the dev proxy — never in the browser or logs). 48 NL→SQL + 6 safety per rung. Results: **L2 in-browser WebGPU = BROKEN** (the 460 MB q4f16 model loads with no OOM — Layer-1 fixed — but degenerates into a repetition loop, `"…The Paper Of The Paper Of…"`; the SAME 0.5B is coherent via Ollama → the fault is the **onnxruntime-web WebGPU/q4f16 path**, isolating the slice-B Layer-3 bug by A/B). **L1 Ollama scales with size:** 0.5B ~10% → 3B 42% intent-correct. **C1 DeepSeek ceiling 92% intent-correct** → the task set is sound + answerable. **Scorer finding:** exact result-set match undercounts badly (penalizes *projection* differences — right rows, different columns); DeepSeek 50% exact → 92% intent-correct once near-misses (row-membership) are counted. **#1 harness fix:** score by key-column membership / the G5 reference-judge. **G3 safety 0/6 leaks on EVERY rung** — the one gate passing cleanly. **Decision:** NOT the "free-AI→BYOK-AI" hard-stop — free Ollama shows size-scaling promise + tasks are answerable; sub-70% is dominated by the scorer artifact + broken WebGPU path. Next: fix the scorer + re-score; dtype A/B (q8/fp16) to un-break WebGPU; test a 7B+ Ollama model; run the semantic half (G4, not yet run). **Runner improvements landed** (OpenAI-compatible transport for L1/proxied-C1 since the custom provider requires https; `__M0_PROBE` single-inference diagnostic; nlLimit/skipSemantic; `serve.mjs` dev proxy injecting the DeepSeek key server-side). Full writeup: `eval/m0/FINDINGS-2026-07-03.md`. **No product code touched** — `npm run check` green, bundle unchanged **707.9 KB**. DECISIONS **BJ**.

## Earlier last-update: 2026-07-03T12:30:00Z — **Facet `embedSearch` module + M0 browser runner BUILT (the remaining Chunk-1 code).** With the harness (dataset + tasks + scoring) already up, built the two code pieces left — everything short of actually running them (WebGPU + key gated). **`src/core/embed-search.ts`** (keeper — the `window.facet.embedSearch` verb / Embedding-view backend): engine-boundary-clean (zero imports; pure logic + SQL emission), with a **DuckDB VSS path** (`buildVssSql` → `array_cosine_similarity` over a precomputed `FLOAT[dim]` column) and a **JS path** (`cosineSimilarity`/`rankBySimilarity`/`embedSearchInMemory`, used by the runner). Injection-safe (quoted idents, finite-validated vector literal, positive-int k). **14 unit tests + the emitted VSS SQL validated against real DuckDB** (ranks correctly, excludes NULL). **Embedding pipeline** added to `src/lazy/transformers.ts` — `loadEmbedder` (`feature-extraction`, all-MiniLM-L6-v2, 384-dim, normalised), mirroring `loadModel`'s split-singleton return pattern. **`eval/m0/runner/`** — a **standalone** harness (`harness.ts`+`.html`+`build.mjs`+`run.mjs`), NOT wired into `main.ts`: it imports the real Engine + `mountFile` + `dispatchJob` + the transformers chunk + `embed-search`, boots DuckDB (CDN), mounts the fixture, and drives NL→SQL on L1/L2/C1 + local embedding on L2 → `results.json`. Rung→provider: L2=`local`, L1=`custom`+Ollama, C1=`anthropic`/`openai`+BYOK. **Chosen standalone** (not a `window.facet` hook in main) so product code stays untouched — non-regressive by construction. **Verified now:** the runner **esbuild-compiles** (1.6 MB) + tsc-checks; embed-search tests + DuckDB VSS validation pass. **Bundle UNCHANGED 707.9 KB** — neither new module is imported by `main`, so the product bundle + smoke are byte-identical. **Run state:** **901 vitest** (+14) · `npm run check` green (engine-boundary 10+10; embed-search is the 10th optional module) · `npm run smoke` green. DECISIONS **BI**. **Next / owed:** run the runner on a WebGPU box (`eval/m0/runner/README.md`) → `score.py results.json`; confirm the wasm engine exposes `array_cosine_similarity` before wiring the product Embedding view to the DuckDB path.

## Earlier last-update: 2026-07-03T11:40:00Z — **Facet M0 free-AI eval harness BUILT (Chunk 1, buildable-now slice).** Stood up the harness that gates BOTH Facet's "free local AI" pillar and NakliData's owed Layer-3 local-inference item — everything that doesn't need WebGPU or a BYOK key. In tracked **`eval/m0/`** (not product code, not bundled). **Dataset:** a real, messy **OpenAlex Deep-Learning citation slice** (2015-2023) — **2,600 papers · 10,638 intra-set citation edges · 9,880 authors**, wrangled to committed Parquet. Real (not synthetic) because the semantic gate needs genuine text with topical structure; deep-learning is citation-dense with clear subtopic clusters (ResNet cited by 699 in-set). **Deliberately ugly schema** (`data/SCHEMA.md`) to stress G1 schema-grounding: cryptic column names, a citation **direction** trap (`src` cites `dst`), a **two-senses "most cited"** trap (global `n_cite` vs intra-set in-degree), a **mixed-type `score`** VARCHAR (needs `TRY_CAST`), + ~24% null abstracts. **85 labeled tasks:** 48 NL→SQL (+6 safety) + 31 semantic-search; every NL→SQL reference SQL **self-validated against the fixture** and made **deterministic** (caught + fixed 3 flaky top-N-with-ties refs — a real eval-soundness bug). Semantic relevance = topical clusters (keyword + eyeball-excluded false positives). **Scoring + 5-gate report** (`scripts/score.py`, self-tested `--selftest`) in Python + native DuckDB (dev-only, same engine as the wasm build — **not** a project/bundle dep); result-set match, precision@k, safety scan, two-judge divergence all compute model-free. **The one owed piece:** the thin **browser-side runner** that drives the sidecar on L1/L2/C1 + local embedding on L2 → `results.json` (contract in `RESULTS_SCHEMA.md`); needs a WebGPU box + BYOK key (can't run headless). **Named escalation unchanged:** G1 clearing only on C1 (BYOK) → stop, restructure the pitch. **No product code touched** — `npm run check` green, bundle unchanged **707.9 KB / 750 KB**; 8 MB raw OpenAlex fetch gitignored (Parquet fixture committed for reproducibility). DECISIONS **BH**. **Next:** run the harness on a WebGPU box (implement the runner → `score.py results.json`).

## Earlier last-update: 2026-07-03T10:50:00Z — **Brave File System Access handling fixed (field reports FR-1 / FR-2).** An intern testing on **Fedora + Brave** couldn't add data: folder mount showed "needs Chrome / Edge / Opera 122+" and a SQLite file upload failed with `NotReadableError` ("The requested file could not be read…"). Root cause: **Brave ships Chromium but gates the File System Access API behind Shields** — `showDirectoryPicker` is absent (FR-1); an FSA file-handle read throws *after* a successful pick (FR-2). Fix in `src/main.ts`: a **synchronous** `isBrave()` (via the Brave-only `navigator.brave` API — sync on purpose so we never `await` before a picker and drop its required user-activation); **FR-1** → Brave-specific toast that names Shields + steers to the working **"Add file"** flow (folder mount stays genuinely unsupported on Brave — no `showDirectoryPicker`, no fallback possible); **FR-2** → on Brave, prefer the existing classic **`<input type=file>`** path over `showOpenFilePicker` (a plain input read isn't Shields-gated). **Non-regressive:** `isBrave()` is false on every non-Brave browser, so all existing paths — incl. the headless-Chromium smoke — are behavior-identical. **Run state:** **887 vitest** · `npm run smoke` green (unchanged Chromium path) · `npm run check` green (my `main.ts` clean; the 24 biome warnings are pre-existing) · bundle **707.9 KB / 750 KB** (42.1 KB headroom). DECISIONS **BG**; no spec amendment (behavior within §1.3's browser floor; Brave was always outside it). **Not unit-tested** (DOM/FSA glue in `main.ts` — codebase convention is pure-logic→vitest, DOM-glue→smoke) and the Brave-Shields failure can't be reproduced headless. **Verification OWED:** the intern must confirm against the live deploy on his actual Brave; if it still fails, the next lever is the `<input>` read itself under Shields. **Next:** unchanged — Chunk 1 (Facet M0 / Layer-3 eval-gate on a WebGPU box).

## Earlier last-update: 2026-07-03T10:30:00Z — **Facet merged in as a NakliData view-type track (planning/decision only — no code yet).** "Facet" — a separate-project concept for a browser-native **graph + embedding explorer** ("one data shape, many views": network / KG / semantic map / temporal / distribution / hierarchy / geo, over an in-browser DuckDB core, crossfilter-linked, removable BYOK sidecar) — folds into NakliData as a new **view-type track** (the Resolve-track pattern). The reason it merges cleanly: its entire v1.0 dependency list is **already installed here** (`@duckdb/duckdb-wasm`, `@huggingface/transformers`, `cytoscape`, `@deck.gl/*` + `maplibre-gl`, `@observablehq/plot`, `@codemirror/lang-sql`) plus the existing `src/core/sidecar` + `src/core/secrets` — Facet is a **view-renderer layer over the substrate we already ship**, not a new app. **Sovereign tier only:** Facet's *commercial* tier (team rooms / relay AI / cloud sync / SSO / a real backend) crosses the Hard-NOT bright line and stays a **separate future repo/company** — not this repo (Facet's own design already split it out). **M0 folds into the owed Layer-3 item:** Facet's riskiest-assumption gate (prove free local L1/L2 AI is useful+safe: schema-grounded loud-failing NL→SQL + useful low-latency local embedding search — not only BYOK) is the *same problem* as our parked local-inference-quality R&D, so one eval-harness pass answers both (WebGPU-gated, can't run headless). **This change is docs/planning only — zero product code touched** (no gate run: `npm run smoke`/`test`/`check` unaffected). Spec amendment **A34** (view-type layer: lazy-loaded, budget-exempt; commercial tier explicitly out of scope); DECISIONS **BE**. Companion working notes in `plan/`: `facet-track-vision.md`, `facet-m0-handoff.md`, `facet-ux-preview.html`. **Graph engine PINNED (DECISIONS BF):** target scale 100k–1M+ with routine-1M force graphs → **deck.gl** render for ALL views (MIT, already bundled, GPU 1M+ primitives) + **`@antv/layout`** (standalone MIT, WASM/WebGPU force layout); graph interactions built on deck.gl. G6-the-framework rejected (1M render ceiling — we take only its layout package); Cosmos rejected (CC-BY-NC, commercial-hostile). Reframe: single-substrate is *not* a moat (engineering convenience) — the unification is the DuckDB + crossfilter data layer, so engine chosen best-renderer-per-view. **Next:** Chunk 1 — the Facet M0 / Layer-3 eval-gate on a WebGPU box (`plan/workplan.md`).

## Earlier last-update: 2026-06-23T17:00:00Z — **Resolve track M3 (golden-table sink) BUILT + tagged `v1.5.2` — the Resolve track is COMPLETE (resolve → segment → own).** The **Own** verb: a new export **sink** (alongside CSV / Parquet / Export-anonymized / KanZen / Bahi / NakliPoster) that collapses a result to ONE row per canonical entity (typically M1's `<col>__merged`) with a per-column **survivorship rule** (keep-first / max / min / latest) and writes the deduped table to a folder you keep as CSV or Parquet. Customer 360, inverted to ownership — a file you hold, nothing pushed to a plane. New pure `src/core/golden.ts` (engine-boundary-clean; `buildGoldenSql` emits `SELECT "entity", first/max/min/arg_max(…) AS col FROM cell_<id> GROUP BY "entity"` via the injection-safe `quoteIdent` — every identifier quoted, the aggregate fn from a fixed allowlist; the entity column is the GROUP BY key, excluded from the aggregates; a 'latest' rule → `arg_max(col, orderCol)`). A Promise-based golden-table modal (entity picker, per-column survivorship selects, an order-column picker shown/required for 'latest', CSV/Parquet, live SQL preview); the sink mirrors the Export-anonymized path (`COPY (<sql>) TO <temp>` → read → folder-write). **No `.naklidata` schema change** (a new sink; writes only to the folder the user picks — Hard NOTs preserved, nothing leaves the tab except into the user's own disk). **Run state:** **887 vitest** (+8 in `tests/golden.test.ts`: per-rule aggregate shape, latest-needs-order throw, entity-excluded, injection-resistant hostile idents, `needsOrderColumn`) · `npm run check` green (tsc + biome + engine-boundary — `golden.ts` is the 9th optional module — + bundle) · `npm run smoke` green (added a live golden-sink assertion: sink registered → modal opens → survivorship SQL preview) · bundle **707.7 KB / 750 KB** (42.3 KB headroom). Spec amendment **A33**; DECISIONS **BD**. **Tagged `v1.5.2` + pushed.** **The Resolve track (M1 clustering → M2 segments → M3 golden sink) is shipped end-to-end.** **Owed (unchanged):** deeper live `/walkthrough`s of each surface (smoke covers the critical paths). **Next:** pick up another portfolio thread, or deepen the track (persisted cluster mappings, multi-column entity keys).

## Earlier last-update: 2026-06-23T15:00:00Z — **Resolve track M2 (segment primitive) BUILT + tagged `v1.5.1`.** A named, reusable **`SEGMENT(name)`** macro — the Audience verb of the Resolve track — that expands to a parenthesized WHERE predicate at the SAME single audited point as `MEASURE()` / `DIM()` (`expandMeasures`, now a 4-macro pass: MEASURE + DIM + SEGMENT). New pure `src/core/segments.ts` (engine-boundary-clean; mirrors `dimensions.ts` — `SegmentDefinition` + `SegmentsStore` + validators reusing the measure keyword/semicolon guard + `findReferencedSegments`). `SELECT * FROM t WHERE SEGMENT(high_value_lapsed)` → `WHERE (total_amount > 100000 AND last_seen < '2026-01-01')`; an unknown segment substitutes `FALSE` (well-formed boolean slot) and is surfaced as `Unknown SEGMENT(x)` BEFORE the cell runs. Managed in the **Semantic layer** panel alongside measures + dimensions (add / list / delete + used-by-N-cells + the **View-as-code** JSON now `{measures, dimensions, segments}`). **Persisted as an optional `segments` field** on `.naklidata` (mirrors how `dimensions` shipped in v1.4 — pre-M2 files round-trip clean; no breaking change). Pure macro expansion — no server, emits a cell the user runs (Hard NOT #4), no auto-run. **Run state:** **879 vitest** (+16 in `tests/segments.test.ts`: name/predicate validation, store round-trip, SEGMENT expansion incl. nesting with DIM + the unknown→`FALSE` path + all-three-macros-in-one-query) · `npm run check` green (tsc + biome + engine-boundary — `segments.ts` is the 8th optional module — + bundle) · `npm run smoke` green (added a live Semantic-panel Segments-section assertion) · bundle **700.8 KB / 750 KB** (49.2 KB headroom). Spec amendment **A32**; DECISIONS **BB–BC**. **Tagged `v1.5.1` + pushed.** **Next:** M3 — golden-table sink (export the resolved canonical-entity table to a file, survivorship rules → v1.5.2).

## Earlier last-update: 2026-06-23T12:30:00Z — **Resolve track M1 (clustering / fuzzy-merge) BUILT — the first Resolve-track surface.** Detect variant spellings of a column's values and emit an **additive CASE-rewrite SQL cell** (`<col>__merged`) the user runs (Hard NOT #4). Two OpenRefine-standard methods: **key collision** (fingerprint — case/punctuation/word-order, default) + **nearest neighbour** (Levenshtein, opt-in, blocked by first-fingerprint-char + an EXACT length-window prune `len_j ≤ len_i/t`, capped at 5,000 distinct values). New pure `src/core/clustering.ts` (engine-boundary-clean; reuses the v1.2 M5 injection-safe `quoteIdent`/`quoteLiteral` emitter — every variant value + canonical `quoteLiteral`'d, the column + alias `quoteIdent`'d; hostile idents/values can't escape quoting). The CASE cell is the artifact — reproducible, replays with no model. **Two entry points:** a "Cluster" chip on every SQL result + a per-column "Cluster values" action in the schema panel. Modal: column picker, method toggle, NN threshold slider, per-cluster editable canonical (default most-frequent→longest→lexicographic) + per-variant counts + accept/reject (rejected **dim, not hidden**), live CASE preview, empty/`tooMany` states. **Removable sidecar job #8 `propose-merge`** (the AI): only on explicit "Ask AI to check ambiguous pairs" (hidden with no provider), fed ONLY borderline pairs (similarity in `[t-0.1, t)`), structured no-prose (three-layer guard like propose-chart), **per-pair allowlist** hallucination guard (blocks fabricated values AND recombined pairings). **Removability holds:** delete the job → key-collision + NN still cluster fully. **No `.naklidata` schema change** (clusters are ephemeral; the SQL cell is the durable artifact). One new dep: `fastest-levenshtein` (~2 KB, MIT). **Run state:** **863 vitest** (+44 in `tests/clustering.test.ts`: fingerprint family/diacritics/token-reorder, singleton-not-a-cluster, NN threshold-boundary + single-link chaining + blocking + cap, canonical tie-breaks, the **CASE-emitter injection battery** (hostile idents + literals), borderlinePairs band, `propose-merge` parser incl. **recombination-reject**) · `npm run check` green (tsc + biome + engine-boundary + bundle) · `npm run smoke` green (added a live cluster-modal assertion: chip → engine GROUP BY → core → CASE-emit path renders) · bundle **696.0 KB / 750 KB** (54.0 KB headroom). **Forward-pass review (§17)** of the core + emitter ran (independent adversarial agent): injection property clean, hallucination guard sound, engine-boundary pure — one hardening applied (per-pair allowlist). Spec amendment **A31**; DECISIONS **AV–AZ**. **Version resolved — tagged `v1.5.0`** (the prior F5/F6 + local-model-fix batch tags as `v1.4.1` at `973d416`; versioning is git-tag-based, `BUILD_VERSION` stays `0.1.0` by convention — DECISIONS BA). Pushed to `origin/main` (deploy fires). **Owed:** a deeper live `/walkthrough` of the accept/reject/edit-canonical + schema-panel-entry + Ask-AI paths (smoke covers the critical path). **Next:** M2 — segment primitive (`SEGMENT(name)`, → v1.5.1) → M3 — golden-table sink (→ v1.5.2).

## Earlier last-update: 2026-06-13T16:30:00Z — **Local-model path FIXED (load + registration) + re-validated in a real WebGPU Chrome; inference-quality issue remains.** Followed the slice-B FAIL with fixes (DECISIONS AU), re-tested live. **Layer 1 (load) FIXED:** WebGPU device path + `q4f16` on WebGPU — the 1-2B models now load (the 1.5B q4f16 is 1.14 GB and fits where plain q4 OOM'd); graceful OOM messaging; model-size labels corrected (~2× understated); **added Qwen2.5-0.5B as the fits-anywhere recommended default**. **Layer 2 (the real blocker) FIXED:** the lazy `transformers.ts` chunk registered the generator into its OWN copy of the `local-runtime` singleton (esbuild `splitting:false`), so the main-bundle dispatch never saw it → "model not loaded" forever (same split-singleton class as DECISIONS AJ). Now the chunk's `loadModel` RETURNS the generator and the MAIN bundle registers it — confirmed: jobs now dispatch to the model. **Layer 3 (still open):** in-browser inference produces **incoherent output** (`{SQL!!!!!!` / `'\'%-*02*'`) across all structured jobs even with the 1.5B — likely an onnxruntime-web WebGPU/q4 numerical or chat-template issue, NOT the sidecar. So **local jobs don't yet yield usable output; a cloud BYOK provider works** (default; eval-tested). Local provider now labelled **experimental**. **Run state:** 819 vitest · check · smoke green; bundle 677.5 KB / 750 KB. Layers 1+2 are real bug fixes (prerequisites); Layer 3 tracked as v1.4.1+ deep-dive in `plan/pending.md`. Full notes: `plan/w32-slice-b-validation.md` + DECISIONS AU. **Next:** ship these fixes; Layer-3 local-inference deep-dive.

## Earlier last-update: 2026-06-13T14:30:00Z — **W3.2 slice-B validation RAN (real Chrome + WebGPU box, via Chrome MCP) → ❌ FAIL.** The owed Chunk 2 validation finally ran against the live build in a real browser. **0/6 sidecar jobs runnable** because the recommended default **Qwen2.5-1.5B fails to LOAD on the wasm device**: `Can't create a session. ERROR_CODE: 6, ERROR_MESSAGE: std::bad_alloc` (reproduced twice, incl. clean load-from-cache — the wasm32 runtime can't allocate the ~1.7 GB q4 weights). **Root cause:** `src/lazy/transformers.ts:262` hard-codes `device: 'wasm'`; the planned WebGPU opt-in was never wired (WebGPU WAS available in the test browser). **Also found:** model-size labels understated ~2× (Qwen "~0.9 GB" → 1.7 GB actual); the failed load freezes the tab ~45 s. **What passed:** download pipeline, OPFS cache list/delete/forget-all (verified to actually free disk — 1.68 GB → 2 MB), partial-download auto-cleanup, settings persistence across a crash. **Disposition:** filed as **v1.4.1** work (DECISIONS AT; `plan/pending.md` "Now open") — wire WebGPU + fix labels + graceful OOM. v1.3.0/v1.4.0 tags stand (local provider is opt-in + off by default, so shipped defaults never hit this path). Full results: `plan/w32-slice-b-validation.md`. **Next:** the v1.4.1 local-model fix (a WebGPU box is available to re-validate).

## Earlier last-update: 2026-06-13T13:00:00Z — **Chunk 3 backlog cleared — parked forward-pass minors + F5/F6 stretches.** Autonomous run through `plan/workplan.md` Chunk 3 (5 commits `df54216`…`9a984a5`). **Fixes:** M17 (query-builder aggregate-without-GROUP-BY silently dropping select columns), M19 (lineage-store orphan inbound edges on cell delete), **M14 (runAll now runs in @name-dependency / topological order, not raw document order)**, M9 (measures-panel Enter-to-add), M10 (window.confirm → reusable `src/ui/confirm-modal.ts`), L1/L2/L3/L4/L5/L12/L14/L15, S1 (deleted dead chart-config validators). **Stretches shipped:** **F5** — calc-field window mode now partitions by MULTIPLE columns (checkbox group; core was already array-capable); **F6** — visual query builder grew **multi-step pipelines** (derived filter→summarise→re-summarise steps; `emitPipeline` nests the base as `FROM (…) AS step_N` reusing the injection-safe emitter; full N-step UI). **Won't-fix (logged DECISIONS AQ):** M11/S3 (the "applicable measures" panel was never built + requiredTypes unauthored), L24 (`?? 0` required by noUncheckedIndexedAccess), L18, S12, S16, and cosmetic L/S items. **Run state:** **819 vitest** (+21: query-builder pipelines, refresh, lineage, notebook-graph topo-sort) · `npm run check` green · `npm run smoke` green · bundle **677.3 KB / 750 KB** (72.7 KB headroom). F5 + F6 Chrome-verified via throwaway Playwright runs (removed). DECISIONS AQ/AR/AS. **Owed (unchanged):** the WebGPU slice-B per-job validation — needs a capable browser, can't run headless. **Next:** slice-B validation on a WebGPU box → a v1.4.1 if it surfaces fixes.

## Earlier last-update: 2026-06-13T11:15:00Z — **v1.4.0 RELEASED + tagged.** Cut the release from the green v1.4 slate. **Tag `v1.4.0`** on `origin/main`. Canonical changelog: `plan/v1.4.0-release-notes.md` — the nine-feature BI-parity slate (F1 named dimensions · F2 Semantic catalog panel · F3 code view · F4 calculated fields · F5 window/LOD · F6 multi-join query builder · F7 X-Ray profile · F8 numeric distribution+outliers · F9 sandboxed-iframe embed), the latent M2 store-singleton fix, and the infra reconcile (Cloudflare Workers deploy + e2e in the CI verify gate). README refreshed with the v1.4 surfaces (Semantic layer, Calc field, X-Ray, Embed, multi-join builder). **No breaking changes** (additive; `dimensions` is an optional `.naklidata` field). **No new spec amendments** — every feature extends an already-amended surface; design decisions in DECISIONS AI–AO; the release cut is DECISIONS AP. **Gates before the tag:** 798 vitest (45 files) · `npm run check` green · `npm run smoke` green · bundle **666.9 KB / 750 KB** (83.1 KB headroom); 55 e2e green at the session-3 windup (no code change since — docs-only release). **Owed (unchanged):** the WebGPU slice-B per-job manual validation (`plan/w32-slice-b-validation.md`) — can't run headless; tracked for a v1.4.1/v1.3.1. **Next:** slice-B validation on a WebGPU box; deferred stretches (F6 multi-step pipelines, F5 multi-column partitions); parked forward-pass minors (good `/replan` candidate).

## Earlier last-update: 2026-06-11T23:00:00Z — **v1.4 feature build COMPLETE — all 9 competitive-analysis candidates (F1–F9) shipped.** F9 (last): an **Embed** header button wraps the self-contained Export-HTML doc (markdown + chart SVGs + tables, no JS) in a **sandboxed `<iframe srcdoc>`** snippet (empty sandbox — no allow-scripts/same-origin, since the export carries no scripts) for read-only, server-free embedding in a wiki/intranet. Chosen over a `?lens=` iframe (which would render empty charts for local-data notebooks + needs a reachable server) — DECISIONS AO. **The full v1.4 slate:** F1 named dimensions · F2 metrics catalog · F3 code view · F4 calculated fields · F5 window/LOD fields · F6 multi-join query builder · F7 X-Ray profile · F8 numeric distribution+outliers · F9 embed widget. Plus a **latent M2 bug fixed** (lazy measures panel had its own store singletons). **Run state:** **798 vitest** (+39 across the v1.4 cores: dimensions, calc-field, query-builder multi-join, embed) · check green · `npm run smoke` green · **55 Playwright e2e green** (re-run locally — no regression across the panel-non-lazy + query-builder rework) · bundle **666.9 KB / 750 KB** (83.1 KB headroom). Every feature Chrome-verified end-to-end. **9 commits** (`d27d5a9`…this) on `origin/main`. **Next:** consider a v1.4.0 release (write notes + tag); the WebGPU slice-B validation remains the only owed v1.3 item.

## Earlier last-update: 2026-06-11T22:15:00Z — **v1.4 F7/F8 — "X-Ray" profile + numeric distribution.** **F8:** `Engine.profileColumn` now adds a five-number summary (min/q1/median/q3/max) + an IQR-rule outlier count for numeric(-castable) columns — computed over `TRY_CAST(col AS DOUBLE)` so it's type-agnostic (one CTE query + a correlated outlier subquery; non-numeric → `numeric: null`). The schema-panel column profile renders a **Distribution** + **Outliers** row. **F7:** an **X-Ray** button on every SQL result inserts a markdown header + a **stats cell** bound to the result and runs it — one-click descriptives + correlation matrix (reuses v1.3 M4). **Run state:** **794 vitest** (F7/F8 are engine/DOM, Chrome-verified like `profileColumn` itself) · check green · smoke green · bundle **662.8 KB / 750 KB** (87.2 KB headroom) · **Chrome-verified**: numeric profile shows quartiles + outliers; X-Ray inserts a stats cell that auto-runs. **v1.4 progress: F1–F8 done; last up — F9 (embeddable read-only widget).** DECISIONS AN.

## Earlier last-update: 2026-06-11T21:30:00Z — **v1.4 F6 — multi-join visual query builder.** Grew the M5 builder toward Metabase's "ask a question": the transient spec's single `join` became `joins[]`; the modal gained a Joins section (add/remove, attach to the source or an earlier join) AND every filter/aggregate column picker is now **table-qualified** (table select + column select spanning in-scope tables) so joined-table columns are reachable. `validateSpec` enforces each join attaches to an in-scope table. **Run state:** **794 vitest** (+2 multi-join cases; 2 existing JOIN tests migrated to `joins[]`) · check green · smoke green · bundle **660.7 KB / 750 KB** (89.3 KB headroom) · **Chrome-verified**: add join → preview shows the JOIN clause → filter row is table-qualified → inserted SQL cell contains the JOIN. Multi-join chaining covered by the `chains MULTIPLE joins` unit test. **v1.4 progress: F1–F6 done; next F7/F8 (X-Ray profile), F9 (embed).** DECISIONS AM.

## Earlier last-update: 2026-06-11T20:45:00Z — **v1.4 F4/F5 — calculated / derived fields shipped.** A "Calc field" button on every SQL result opens a modal: **Expression** mode (free expression + column chips) or **Window (LOD)** mode (fn + column + optional partition). Emits a NEW SQL cell — `SELECT *, (<expr>) AS "<alias>" FROM (<upstream_sql>) AS calc_src` — the user runs it (Hard NOT #4). New `src/core/calc-field.ts` reuses the M5 injection-safe emitter (`quoteIdent` for the alias, `validateMeasureExpression` for the expr); the F5 window builder is safe-by-construction (fn allowlist + quoted idents). **Run state:** **792 vitest** (+11 in `tests/calc-field.test.ts`) · check green · smoke green · bundle **658.6 KB / 750 KB** (91.4 KB headroom) · **Chrome-verified**: expression field (`total_amount * 2 → amt_x2`) + window field (`SUM(total_amount) OVER (PARTITION BY vendor_name) → vendor_total`) both insert + execute. **v1.4 progress: F1–F5 done; next F6 (guided query builder), F7/F8 (X-Ray), F9 (embed).** DECISIONS AL.

## Earlier last-update: 2026-06-11T20:00:00Z — **v1.4 build started — F1/F2/F3 (semantic layer) shipped + a latent M2 bug fixed.** Building the 9 competitive-analysis feature candidates (`plan/feature-candidates.md`). **F1 — named dimensions:** new `src/core/dimensions.ts` (DimensionDefinition + store, mirrors measures); `expandMeasures` now expands `DIM(name)` in the SAME pass as `MEASURE(name)` via an optional `dimensions` param (back-compat) + an `unknownDimensions` result field. Dimensions persist in `.naklidata`. **F2 — metrics catalog:** the measures panel is now a **Semantic layer** panel (header button relabelled "Semantic") managing BOTH measures + dimensions (add/list/delete + "used by N cells"). **F3 — code view:** a "View as code" toggle shows the layer as editable JSON (`{measures, dimensions}`) with Apply-validation — the store shape, not a new DSL (DECISIONS AK). **Latent M2 bug fixed (DECISIONS AJ):** the measures panel was a lazy chunk, and esbuild's `splitting:false` made it self-contained — it bundled its OWN copy of the stores, so `getMeasuresStore()` in the panel ≠ the notebook's. **Panel-defined measures never reached the expander — broken since v1.3 M2** (no e2e covered it). Fixed by importing the panel directly into `main` (non-lazy); removed `src/lazy/measures-panel.ts`. **Run state:** **781 vitest** (+11 in `tests/dimensions.test.ts`) · check green · smoke green · bundle **651.2 KB / 750 KB** (+13.5 from the inlined panel; 98.8 KB headroom) · **Chrome-verified** end-to-end: add dimension + measure via the panel → code view shows JSON → `SELECT DIM(gstin_state), MEASURE(total) FROM invoices GROUP BY 1` expands + executes (5 rows). **Next:** F4 (calculated fields), then F5–F9.

## Earlier last-update: 2026-06-11T18:30:00Z — **v1.3.0 RELEASED + tagged + deployed.** Cut the release after the Phase 2 UI completed. **Tag `v1.3.0`** at `87b7c49` on `origin/main`, live at https://naklitechie.github.io/NakliData/. Canonical changelog: `plan/v1.3.0-release-notes.md` — covers the v1.2 Lakehouse Parity track (M1–M5), the v1.3 Prior Art track (M0–M6), the Phase 2 UI (cross-filter grey-out + associations panel + shelf authoring + lineage edit mode), the forward-pass hardening audit + CI verify gate, and the W3.2 slice-B local runtime. **No breaking changes** (additive; bundle cap 600→750 KB is the only config change, A30). README refreshed with the new surfaces. **Gates before the tag:** **55 Playwright e2e green** (run locally; `npm run test:e2e` — Chunk 3's e2e half, DECISIONS AH) · 770 vitest · check · smoke · bundle 636.5 KB / 750 KB. **Decision (AG):** shipped WITHOUT the WebGPU slice-B per-job manual validation — it can't run headless and the session was autonomous/user-away; the local-model CODE is unit-tested + opt-in, so it can't regress shipped defaults; validation stays a tracked v1.3.1 follow-up. **Chunk 4 parked minors (selective):** **M12** (addCell fell through to `chart` for an unknown kind → now an exhaustiveness `never` guard that throws) + **L6** (selection-bar amber palette was hardcoded hex → token-derived `color-mix(var(--warning))` in CSS). Most other parked M/L/S items were already closed (M13 picker, M23 CI bundle gate, M24/C2 = A30) or are pattern-notes/intentional; the rest remain low-priority parked for a future cleanup pass or `/replan`. **Next:** Chunk 3's remaining half — **W3.2 slice-B manual validation** (needs a WebGPU browser; checklist in `plan/w32-slice-b-validation.md`) → then a v1.3.1 if it surfaces fixes. Optionally wire e2e into the CI verify job (DECISIONS AH).

## Earlier last-update: 2026-06-11T17:00:00Z — **v1.3 M1 manual-associations panel shipped — v1.3 Phase 2 UI is now fully complete.** The cross-filter now spans cells. New **Associations** header button opens a modal (hybrid authoring, user-chosen): **auto-suggested** links (columns sharing a taxonomy type or name across cells) + a **manual** "link two columns" form + an active-links list with unlink. New `src/core/associations.ts` (engine-boundary clean): an `AssociationsStore` (links of two `(table,col)` keys; persisted to `.naklidata` as the optional `associations` field, mirrors `selections`), `suggestAssociations` (by type + name), and the compute keystone `resolveEffectiveSelectionsForTable` — which UNIONs a column's own selection with values selected on any transitively-associated column. **Compute model (DECISIONS AE):** inter-cell cross-filter REUSES the intra-cell engine — an association just propagates selected values onto associated columns, then each cell paints from its own materialised rows via `computeValueStates`. **No engine round-trip** (same in-memory posture as the M1 grey-out). `paintResultSelectionStates` now resolves effective selections; `main.ts` repaints on association-store ticks. **Run state:** **770 vitest** (+16 in `tests/associations.test.ts`: store CRUD/round-trip, propagation incl. transitive cluster, suggest by type/name) · `npm run check` green · `npm run smoke` green · bundle **635.8 KB / 750 KB** (114.2 KB headroom) · **Chrome-verified** (throwaway, removed): two cells sharing `vendor` → modal suggests the link by name → after linking, selecting `vendor=Acme` in cell A propagates to cell B (Acme highlighted, `region=south`/Initech greyed, `region=north` kept). **Bundled in this push:** a separate fix commit (`de1a309`) closing the lineage bug flagged during M6 — source→cell edges were empty because duckdb-wasm 1.29.0 inlines view-backed mounts; the plan walk now unions a CTE-aware SQL sniff. **Next:** **v1.3.0 release** — Phase 2 UI is done; remaining gates are Chunk 3 (W3.2 slice-B validation, needs WebGPU) + a one-off `npm run test:e2e`.

## Earlier last-update: 2026-06-11T15:30:00Z — **v1.3 M6 Phase 2 UI shipped — lineage edit mode is now user-visible. Phase 2 UI is feature-complete (M1 + M5 + M6 done); only the M1 manual-associations panel remains.** The lineage panel gains an **Edit | Done** toggle (header). In edit mode the LIST view (the panel's "accessible truth"; the SVG stays read-only) grows per-node **Delete** controls + per-edge **+ insert** controls (cell-kind select → sql/chart/pivot/stats/report). Both wire to the existing `applyCanvasOp` (`src/core/lineage-edit.ts`): **insert-on-edge** splits an edge with a new cell node carrying its `cellKind`; **delete-node** removes a node + its edges and, per handoff §M6, lists dependents (`getDependentsOfNode`) in an inline confirm strip BEFORE deleting. Each op is applied to a working graph and persisted via `getLineageStore().loadFromJson(...)`, then the panel body re-renders. **`reposition` deliberately deferred** — it's a layout-only no-op in the core (no layout-hint persistence exists; the SVG auto-layouts by topological depth), so wiring drag-to-move would mean building + persisting a layout-hints layer for zero graph effect (DECISIONS AC). **Caveat (documented inline + in the edit hint):** the canvas is a *projection* — re-running a cell re-derives its inbound edges from EXPLAIN and overwrites that cell's canvas edits; materialising graph edits back into notebook cells is the §M6 follow-up. **Run state:** **745 vitest** (M6 core already covered by `tests/lineage-edit.test.ts`; the new work is DOM) · `npm run check` green · `npm run smoke` green · bundle **627.1 KB / 750 KB** (+4.5 KB, 122.9 KB headroom) · **Chrome-verified** (throwaway, removed): two-cell `@ref` chain → Edit → insert a pivot on the `base→B` edge (adds `cell_ins_0`) → delete it (confirm strip lists the orphaned downstream cell) → cancel keeps it, confirm removes it. **Flagged for follow-up (spawned task):** a single source-reading query (`SELECT * FROM invoices`) produced an EMPTY lineage graph against the offline DuckDB build — only the `@name` cellRef path populated it, suggesting `extractInputsFromPlan` may not match the vendored build's EXPLAIN JSON (and the regex fallback doesn't fire when EXPLAIN returns zero inputs). Out of scope for M6; needs investigation (possible silent M2 regression). **Next:** M1 manual-associations panel (needs a scope decision — surfaced to the user), then v1.3.0 release.

## Earlier last-update: 2026-06-11T14:30:00Z — **v1.3 M5 Phase 2 UI shipped — shelf-based chart authoring is now user-visible.** The chart cell gains a **Manual | Shelves** mode toggle. Shelves mode renders a draggable field tray + **Columns / Rows / Color** drop-zones (HTML5 DnD + a per-shelf `<select>` fallback for keyboard/a11y + an `×` to clear). Dropping a field COMPILES the shelf state to the canonical `ChartConfig` (`compileShelvesToConfig`) and writes it to the SAME cell fields the manual dropdowns use (`xColumn→x`, `yColumn→y`, `groupColumn→facet`) — the Transparency Rule: two editors over one state, so the chart re-renders identically regardless of producer. New pure `inferFieldClass(column, rows)` in `src/core/chart-shelves.ts` classifies each result column from its **materialised data** (not source-table taxonomy — aggregated cols like `COUNT(*) AS n` aren't in any table), driving the taxonomy default matrix (temporal→line, categorical→bar, numeric→scatter, identifier→reject) + the "teach, don't fail" warnings. Author mode is a **session-ephemeral** module map (a view pref, not data — the shelf state is a projection of the cell config). Compile warnings: persistent ones (numeric-on-color, where the field is kept) show inline in the shelf readout; a **rejected** field (identifier-on-y, dropped from config) can't live on the shelf to carry its warning, so it's surfaced via a new reusable `naklidata:toast` window-event bridge (cells can't import the main-local `toast`). **Run state:** **745 vitest** (+8 in `tests/chart-shelves.test.ts` for `inferFieldClass` incl. the id-on-y→warn→COUNT-bar compile path) · `npm run check` green · `npm run smoke` green · bundle **622.6 KB / 750 KB** (+7.5 KB, 127.4 KB headroom) · **Chrome-verified** (throwaway, removed): add chart cell → bind input → Shelves mode → assign x=payment_status + y=total_amount → "→ bar chart" readout + SVG renders; identifier-on-y (`vendor_gstin`) rejected + teach toast; numeric-on-color kept + persistent readout warning. **Next:** M6 lineage edit-mode toggle (last Phase 2 UI surface), then v1.3.0 release.

## Earlier last-update: 2026-06-11T13:00:00Z — **v1.3 M1 Phase 2 UI shipped — associative cross-filter grey-out is now user-visible.** The v1.3 M1 selection store + `computeValueStates` + `buildIntraTableSelectionPredicate` shipped data-only at `a0fa5cf`; this wires the **grey-out renderer** so clicking a value in a SQL result paints the associative states the spec calls for. New pure primitive `computeIntraCellValueStates(columns, rows, selections)` in `src/core/selections.ts` (engine-boundary clean — no DOM/FSA/globals) computes per-(column,value) `ValueState` over the cell's full **materialised** result in JS: v1 scope is **intra-cell** (the "table" is `cell_<id>`, rows are in memory), so co-occurrence needs no engine round-trip. UI binder `paintResultSelectionStates(tableEl, cell)` in `src/ui/cells/sql-cell.ts` formats rows + toggles `xf-selected` (highlight) / `xf-excluded` (greyed, **not hidden** — absence-as-signal) td classes; called both on initial table render and as a **surgical repaint** from a new selections-store subscriber in `main.ts` (`repaintSelectionStates` — per-td class toggles, **not** a full `renderNotebook`, so clicking a value never scroll-jumps the table or steals editor focus). CSS in `notebook.css.ts` uses tokens only (`color-mix(var(--warning))` for selected, opacity + `--text-muted` for excluded) + a hover affordance. Demo mode paints nothing (click-to-select already stripped — H14). **Run state:** **737 vitest** (41 files, +5 new in `tests/selections.test.ts` covering the new primitive: no-selection null, multi-column AND intersection, self-column-adds-no-constraint, sparse rows) · `npm run check` green (tsc + biome + engine-boundary + bundle) · `npm run smoke` green · bundle **615.1 KB / 750 KB** (134.9 KB headroom, +1.7 KB) · **Chrome-verified** via a throwaway Playwright script (since removed): a deterministic VALUES fixture, click `vendor=Acme` → Acme highlighted, `status=void` (never co-occurs) greyed, co-occurring `paid`/`open` kept, bar shows "vendor: Acme"; re-click clears everything + hides the bar. **Next (per `plan/workplan.md`):** M5 shelf drop-zones + M6 lineage edit-mode toggle (rest of Phase 2 UI), then v1.3.0 release.

## Earlier last-update: 2026-06-11T09:00:00Z — **Forward-pass audit closed (Chunks 1–6) — 3 Critical · 16 High · ~30 supporting findings fixed across 6 commits.** Autonomous follow-through on `plan/forward-pass-2026-06-10.md`. **Chunk 1 (`06ae2aa`):** spec amendment A30 + STATUS/DECISIONS hygiene (C2). **Chunk 2 (`196ab28`):** CI gate — `deploy.yml` now runs a `verify` job (build → check → test → smoke) on every push + PR before deploy; engine-boundary `WATCHED_OPTIONAL` extended (M22); smoke `waitUntil:'load'` (L19); postinstall fetch timeouts (L20). **Chunk 3 (`8568530`):** C1 (Explain-error button un-hidden — was unreachable since v1.1; **Chrome-verified**), C3 (real round-trip invariant + `lineageGraphFromJson` validator), H1 (FSA handle IDB leak), H2 (lens modal surfaces embedded SQL; **Chrome-verified**), H3 (2 MB gzip-bomb cap on `?lens=`), H4 (lineage walk cycle guard), H5 (non-finite LIMIT). **Chunk 4 (`f4fd713`):** v1.3 Phase 2 — H8 (smoke covers stats+report), H9 (stats persistence), H10/H11 (report print scoping + cell-ref embedding; **Chrome-verified**), H12 (LineageNode.cellKind), H13 (type-correct cross-filter literals), H14 (demo-mode click-to-select strip), H15 (stats upstream name), H16 (modal-open teardown guards). **Chunk 5 (`047e8a7`):** 19 query/stats/measures corner cases (H6/H7 + 17 M-items). **Chunk 6 (`4fe90b9`):** stray cleanup (S4/S5/S6/S7/S8/S14/S15 + L8/L13/L16/L22). **Run state:** 732 vitest (41 files, +37 over the session start) · `npm run check` green (tsc + biome + engine-boundary + bundle) · bundle **613.4 KB / 750 KB** (137 KB headroom) · `npm run smoke` green (now covers stats + report) · the new CI verify job will run the same suite on push. **Owed:** e2e (55 Playwright) not re-run locally this pass — covered by vitest + smoke; the CI verify job does not run e2e (separate `test:e2e`). 6 commits ahead of `origin/main` (not yet pushed — push triggers the verify gate + GH Pages deploy). Per-chunk detail in `plan/workplan.md`.

## Earlier last-update: 2026-06-11T04:00:00Z — **v1.3 Prior Art track complete (M0–M6) + v1.2 Lakehouse Parity (M1–M5) + forward-pass audit — 18 commits in one session.** The single largest build session to date. Closed BOTH the v1.2 "Lakehouse Parity" handoff (M1→M5: anonymized export, cell lineage, incremental refresh, sidecar auto-viz, visual query builder) AND the v1.3 "Prior Art" handoff (M0→M6) on `origin/main`, then ran a fresh-eyes `/forward-pass` audit. **v1.3 milestones:** M0 `a72f8d1` (engine extraction-readiness sweep + lint boundary), M2 `032e270` (Measures Layer — semantic metrics via DuckDB `FILTER (WHERE …)` aggregates), M1 `a0fa5cf` (Associative Cross-Filter — selection store + persistence; **this commit also raised the bundle cap 600→750 KB**), M4 `38406b6` (Stats cell — descriptives + correlation matrix), M3 `5542fa7` (Report layout + browser print-to-PDF), M5+M6 `057f034` (shelf-based chart authoring + lineage edit mode). **v1.3 M1/M3/M5/M6 shipped as "Phase 1 pure logic + tests"** — the data layer + every load-bearing test invariant is in; the user-visible UI wire-up is **Phase 2, deferred**: M1 grey-out renderer, M3 cell-ref embedding (`H11`), M5 shelf drop-zones, M6 edit-mode toggle, manual-associations panel. Bundle headroom (142 KB on the raised cap) makes Phase 2 ships comfortable. **Engine-boundary contract = pure-logic-only** (no DOM/FSA/browser globals): refactored `taxonomy/load.ts` to inject the fetcher, moved `anonymize.ts` to `src/core/`, extracted `ChartConfig` to `src/core/chart-config.ts` so the M5 "one schema, three producers" rule has a single source of truth. **Forward-pass audit** (`plan/forward-pass-2026-06-10.md`, uncommitted — `plan/` is gitignored): 3 Critical · 16 High · 33 Medium · 24 Low · 18 Stray + 6 verified false positives, batched A–F into `plan/workplan.md`. **This update closes Batch A keystone (forward-pass C2):** spec amendment **A30** now written (`plan/spec-amendments.md` — documents the 600→750 KB cap raise the bundle script already pointed at), this STATUS entry, and the DECISIONS `## 2026-06-11 — v1.3 close + bundle budget raise` entry. **Run state:** 695 vitest passing (35 files) · 55 Playwright e2e green (run locally — **not yet in CI; forward-pass M20/M21**) · bundle **607.4 KB / 750 KB** (A30 raised cap; 142 KB headroom) · engine-boundary lint green (10 required + 4 optional modules; **M22:** `chart-shelves.ts` + `lineage-edit.ts` still owed in `WATCHED_OPTIONAL`) · `npm run smoke` green (but **H8:** zero coverage of v1.3 M3/M4/M5 surfaces). Tree clean except `plan/`. **Next (per `plan/workplan.md`):** Chunk 2 (CI infra — `deploy.yml` ships a red gate today), then Chunk 3 (high-severity correctness/security: C1 hidden Explain-error button, H1–H5 FSA leak + lens SSRF + decompression cap + cycle guard + non-finite limit), then Chunk 4 (v1.3 Phase 2 UI gaps).

## Earlier last-update: 2026-06-11T02:00:00Z — **v1.2 M5 (Visual Query Builder) shipped — v1.2 Lakehouse Parity track complete.** Fifth and final milestone. Form-based UI emits SQL via a STRICT no-string-concat-injection emitter (`src/core/query-builder.ts`). **Per handoff §M5 hard-NOT**: NOT porting the review's `compileVisualQuery` — that was string-concat injection city. Built from `quoteIdent` (wrap in `"`, double internal `"`) + `quoteLiteral` (wrap in `'`, double internal `'`) + per-type validator primitives. Numeric: must parse as a finite number, emitted bare; hostile `1; DROP` becomes NaN → filter SILENTLY DROPPED (defensive — emitted SQL is well-formed without that predicate). String: emitted via `quoteLiteral`; hostile `' OR 1=1; --` becomes `''' OR 1=1; --'` — one quoted literal, never a free SQL fragment. Date: validated against ISO-8601 regex; non-matching values drop the filter. Boolean: validated as `'true' | 'false'`; anything else drops the filter. LIMIT defaults to 100; cap at 1M; throws on < 1. **Scope deliberately small** (handoff): single source table + optional single JOIN on a single key + AND-joined filters + ORDER BY (single column) + LIMIT + GROUP BY + aggregates (SUM/AVG/COUNT/MIN/MAX). NO multi-join, NO nested subqueries, NO window functions — enforced at the spec type level. **UI**: "Build query" header button → modal with source-table dropdown, filter rows, aggregate rows, LIMIT input, live SQL preview pre-block. Source table change carries over filters with column re-validation (handoff explicit gate case). "Insert as SQL cell" drops the emitted SQL into a new SQL cell — user clicks Run (Hard NOT #4). 581 vitest (+27 new in `tests/query-builder.test.ts` — empty-form gate case + numeric filter + LIKE filter + IS NULL + JOIN with no subquery + aggregation + sort + LIMIT clamping + **6 injection-resistance cases** covering hostile table names, column names, single-quote values, hostile numeric values, hostile date values, and control characters) / 55 e2e / smoke / check / bundle **590.6 KB / 600 KB (98.4%, 9.4 KB headroom)** — UNDER the 21 KB budget M4 flagged. Spec amendment A29. DECISIONS 2026-06-10 entries S-U cover the load-bearing choices: `quoteLiteral` + per-type validator path vs PREPARE/EXECUTE (SQL cells take strings; airtight escaping is structurally equivalent to binder protocol), hostile-numeric silently drops vs throws (defence-in-depth narrative; the live SQL preview surfaces the dropped filter to the user), 27 vitest covers the load-bearing surface, e2e for the form-side workflow is a deferred follow-up. Commit `5871b53`. **v1.2 Lakehouse Parity track complete — M1 → M5 closed in this autonomous session over ~6 hours: M1 `391c19c` (anonymized export), M2 `0bb279e` (cell lineage), M3 `42be469` (incremental refresh), M4 `4be16c6` (sidecar auto-viz), M5 `5871b53` (visual query builder). Each milestone shipped with full gate artifacts: STATUS update, DECISIONS entries (3-5 per milestone), spec amendment, README entry, tests green, bundle under budget. 581 vitest total / 55 e2e / smoke + check green on every gate.**

## Earlier last-update: 2026-06-11T00:30:00Z — **v1.2 M4 (Sidecar Auto-Visualization) shipped — fourth milestone of v1.2 Lakehouse Parity track.** Adds the 7th sidecar job, `propose-chart`: SQL result → strict structured `ChartProposal` (chartType from 8-value allowlist + xColumn / yColumn / groupColumn from input columns + title ≤ 80 chars). **NO PROSE** — handoff §10 Hard NOT #6 preserved at three layers: system prompt explicitly bans narration; parser is strict JSON-only (markdown-fence-tolerant but prose-preface-rejecting); response schema has no observation field. **Hallucination guard is all-or-nothing**: any of xColumn / yColumn / groupColumn referencing a column NOT in the input list → drop the whole proposal (return `{proposal: null}`). Per DECISIONS entry Q: a partial-hallucination would have the chart cell render empty or error; binary reject is simpler + teaches the model strict adherence. **UI**: "Suggest chart" chip next to existing "Summarise" chip on every SQL/cohort/assertion result. Click → ship columns + 10 sample rows (10 not 5 per DECISIONS entry R — cardinality signal) → on proposal, `nb.addCell('chart')` + patchCell with field-by-field mapping (`groupColumn → facet` — only rename at the boundary, per DECISIONS entry P). On `proposal: null`: toast "Couldn't propose a chart — try inserting one manually." 554 vitest (+18 new in `tests/propose-chart.test.ts` — 4 happy-path including markdown-fence tolerance + all 8 chart types iteration, 11 rejection including 3 hallucination cases + title length cap + prose preface, 3 prompt-shape) / 55 e2e / smoke / check / bundle **578.7 KB / 600 KB (96.5%, 21.3 KB headroom)**. Spec amendment A28. DECISIONS 2026-06-10 entries P-R cover the load-bearing choices: 1:1 schema mapping with chart cell (`groupColumn → facet` rename at boundary), all-or-nothing hallucination guard, 10-row sample for cardinality signal. Commit `4be16c6`. **Bundle headroom is tight at 21 KB**; M5 visual query builder must come in under that or trim elsewhere (the modal will need to be lean — captured as a constraint). **Next:** M5 — Visual Query Builder (filter / sort / limit / single-join / aggregation; bound parameters via prepared statements, no string concat).

## Earlier last-update: 2026-06-10T23:30:00Z — **v1.2 M3 (Incremental Refresh) shipped — third milestone of v1.2 Lakehouse Parity track.** User-initiated source-change-detection. New "Refresh" header button between "Lineage" and "Settings" → runs one change-detection sweep. **Per-source fingerprint** (`src/core/refresh.ts`): FSA folder aggregates `file.size` SUM + `file.lastModified` MAX across all files via the directory handle (cheap — `getFile()` returns size+lastModified without reading contents); HTTP fires a HEAD request → ETag + Last-Modified + Content-Length; S3 / Iceberg / Bridge stub as `unsupported` sentinel that ALWAYS compares equal (never produces false stale claims; real fingerprinting deferred per DECISIONS 2026-06-10 entry M). Permission for FSA is checked via `queryReadPermissionQuiet` only — never prompts (per DECISIONS entry O). **Diff vs persisted** (IDB; key `refresh:<sessionId>:fingerprints` in the existing kv store — no schema bump). **Cascade via M2 lineage graph** (handoff §M3 explicit dependency on M2): BFS forward from stale sources → cell IDs that need re-running. Cycle-safe via `visited` set. **Result modal** shows three sections: sources that changed (yellow badge), affected cells (blue badge), couldn't check (gray badge). Clean state shows a green check. **Re-run flow**: persist fingerprints FIRST (so the next check has a new baseline), then `Notebook.runCell` each stale cell (which triggers M2 lineage recompute as a side effect, closing the loop). 536 vitest (+25 new in `tests/refresh.test.ts` — 13 fingerprint-equality cases incl. all 5 source kinds + diamond + 3-hop + cycle cascade + 3 constructor helpers) / 55 e2e / smoke / check / bundle **574.1 KB / 600 KB (95.7%, 25.9 KB headroom)**. **Hard NOTs preserved**: NO background polling (no setInterval anywhere in the bundle for refresh — verified by code review; this is handoff §10), NO persistence of file contents (only tokens), NO auto-rescan on boot (deferred opt-in). Spec amendment A27 in `plan/spec-amendments.md`. DECISIONS 2026-06-10 entries K-O cover the load-bearing choices: discriminated-union fingerprint shape, M2-lineage-as-cascade-engine, deferred iceberg/bridge/s3 fingerprinting, persist-after-confirm ordering, query-only permission policy. Commit `42be469`. **Next:** M4 — Sidecar Auto-Visualization (structured ChartProposal config only, no prose, opt-in proposal chip).

## Earlier last-update: 2026-06-10T22:00:00Z — **v1.2 M2 (Cell Lineage Tracker) shipped — second milestone of v1.2 Lakehouse Parity track.** Every SQL/cohort/assertion cell now records its upstream inputs (mounted tables, upstream cells, inline file paths) into a workbook-level lineage graph after each successful run. **Extraction is EXPLAIN-based, not regex** (handoff §M2 + §10): `Engine.explainPlan(sql)` runs `EXPLAIN (FORMAT JSON) <sql>` and `extractInputsFromPlan` walks the JSON tree pulling table names from `SEQ_SCAN`/`INDEX_SCAN`/`TABLE_SCAN` ops and file paths from `READ_PARQUET`/`READ_CSV[_AUTO]`/`READ_JSON[_AUTO]` ops. **Two gate cases from the handoff lock in correctness:** (1) CTE-shadow safety — `WITH vendors AS (...) SELECT * FROM vendors` does NOT emit a vendors edge because the planner expands the CTE into a `CTE_REF` operator (which is on the IGNORE list), not a SEQ_SCAN; (2) Inline-table-function — `FROM read_parquet('/p/x.parquet')` captures as a `file:/p/x.parquet` input (regex would have missed it entirely). `extra_info` shape accepted in BOTH forms — object (`{Table: "vendors"}`) and string (`"vendors\n[Projections: ...]"`). Regex fallback exists only for cells whose SQL didn't parse; tagged `confidence: 'low'`. **Graph state** (`src/core/lineage-store.ts`) has 3 node kinds — source / cell / sink. Per-cell `setCellInputs` REPLACES inbound edges (handoff §M2 incremental update — "delete stale edges for the edited cell only"). **Persistence**: new `lineage` field on `NakliDataFile`; pre-M2 files round-trip cleanly. **UI** (handoff §6 + §10): "Lineage" header button opens a panel with BOTH a list view (load-bearing accessible truth, keyboard-traversable) AND a hand-rolled SVG (enhancement, three-lane layout with sources / cells / sinks, longest-path topological depth, no D3 / no React-Flow per handoff §10). 511 vitest (+29 new in `tests/lineage.test.ts` — 16 plan-parser cases + 6 regex-fallback cases + 7 graph-store cases) / 55 e2e / smoke / check / bundle **566.4 KB / 600 KB (94.4%, 33.6 KB headroom)**. Spec amendment A26 in `plan/spec-amendments.md`. DECISIONS 2026-06-10 entries F-J cover the load-bearing choices: EXPLAIN sufficiency check (no escalation per handoff §11), `extra_info` shape portability, explicit IGNORE_OPS list for CTE-shadow safety, best-effort lineage failure handling, 3-node-kind model. Commit `0bb279e`. **Next:** M3 — Incremental Refresh (IDB fingerprint diff, manual rescan only, no auto-polling).

## Earlier last-update: 2026-06-10T18:00:00Z — **v1.2 M1 (Anonymized Export Sink) shipped — first milestone of v1.2 Lakehouse Parity track.** User pasted `NAKLIDATA-AGENT-HANDOFF-v1.2.md` with "start building autonomously in sequence" — this is M1 of M1→M2→M3→M4→M5. Sixth sink "Export anonymized" sits between Parquet and Kanzen in the sink picker. Per-column strategies: `keep` (verbatim) / `hash` (`md5(COALESCE(CAST("col" AS VARCHAR), '') || '<salt>')`) / `redact` (`'[REDACTED]'` literal) / `bucket` (numeric: floor-to-100; date: DATE_TRUNC month; misbadged-string: falls back to redact rather than CAST-error at runtime) / `drop` (omit from projection). Defaults driven by §3.2 sensitivity badges (A15): pii→hash, financial→bucket, secret→redact (handoff §M1 implies; stricter than hash because the hash is itself a fingerprint), public→keep. Salt: 16 random bytes via `crypto.getRandomValues`, hex-encoded, shown once with Copy + Regenerate affordances, never persisted. Same-salt re-export = paste-it-back. Manifest JSON written alongside the data file (separate save-file picker, cancelling skips manifest but ships data) — `format: 'naklidata-anonymize-manifest'`, columns + strategies + saltUsed boolean (NEVER the salt value), notes blurb. **SQL projection rewrite, not JS post-processing** — handoff §10 explicitly rejects the review's `compileVisualQuery` string-concat pattern; every identifier flows through `quoteIdent` (wrap in `"`, double internal `"`), every literal through `quoteLiteral` (wrap in `'`, double internal `'`). 34 new vitest cases in `tests/anonymize.test.ts` prove the projection holds against hostile column names (`'`, `"`, `;`, `--`), hostile salts (single quote in salt), and the bucket→redact fallback. **MD5 vs SHA-256**: chose MD5 — threat model is "recipient-side re-identification via lookup," not collision resistance; salt does the load-bearing work; 32-char output halves the column width. 482 vitest (+34 new) / 55 e2e / smoke / check / bundle **551.0 KB / 600 KB (91.8%, 49 KB headroom)**. Spec amendment A25 in `plan/spec-amendments.md`. DECISIONS 2026-06-10 entries A-E cover the load-bearing choices. Commit `391c19c`. **Next:** M2 — Cell Lineage Tracker (EXPLAIN-based, hand-rolled SVG, no D3 per handoff §10).

## Earlier last-update: 2026-06-03T10:40:00Z — **W3.2 slice B (local Transformers.js inference) code shipped.** Four autonomous chunks across `87b56a1` → `6e8fed4`: (1) OPFS-backed cache primitive (`src/core/sidecar/local-cache.ts` + 19 vitest cases for pure-helper + no-OPFS-bail coverage); (2) Transformers.js lazy chunk + `env.customCache` OPFS adapter + pipeline + `generate()` matching the `LocalGenerator` contract (`src/lazy/transformers.ts`, `dist/chunks/transformers.js` = 525 KB after tree-shake; depends on `@huggingface/transformers@^4.2.0`); (3) Settings UI — new 4th provider radio (Local), curated 3-model list with byte-size/license/quality hints, Download & load button with live progress, cached-models list with size + per-row delete, Forget-all-cached affordance, no-OPFS graceful bail; (4) boot-path auto-load — when `provider === 'local'` + weights already in OPFS, the chunk + pipeline load on boot with a toast. **Curated models** (`LOCAL_MODEL_OPTIONS`): Qwen2.5-1.5B-Instruct (~0.9 GB, Apache 2.0, default), Phi-3.5-mini-instruct (~2.3 GB, MIT, best quality), Llama-3.2-1B-Instruct (~0.7 GB, Meta, smallest). Decisions K-O in DECISIONS.md cover custom OPFS cache vs Cache API, curated list vs free-text, conditional auto-load (only when cached), wasm-default device, validation-gated v1.3.0 tag. **Still owed**: chunk 5 (per-job manual validation of all 6 sidecar jobs against the loaded model — checklist in `plan/w32-slice-b-validation.md`), then chunk 7 (v1.3.0 tag) once 6/6 PASS. 448 vitest (+27 over v1.2.2's 421) / 55/55 e2e / smoke / check green. Bundle 541.1 KB / 600 KB (90.2%, 58.9 KB headroom). Lazy chunks: codemirror 370 KB, transformers 525 KB (NEW), maplibre-map 1.0 MB, cytoscape-graph 443 KB, deckgl-points 620 KB. Detail: `plan/w32-slice-b-scoping.md` (decisions), `plan/w32-slice-b-validation.md` (chunk 5 checklist), spec amendment A24.

## Earlier last-update: 2026-06-02T22:30:00Z — **v1.2.2 tagged — security-and-hardening sweep.** A whole-codebase `/forward-pass` audit produced 33 findings (1 Critical / 8 High / 15 Medium / 9 Low). Closed across 8 themed batches (A: XSS + CSP keystone; B: sidecar guardrails; C: lens SSRF + iceberg + bearer-token CRLF; D: supply-chain + dev-server; E: chart+classifier correctness; F: engine housekeeping; G: modals+lifecycle; H: build+SW+misc). A two-track adversarial review (internal code-reviewer agent + external `codex review --base v1.2.1`) then found **9 more bugs in those very fixes** — 2 HIGH (NL→SQL replacement-scan via single-quoted FROM bypass + LATERAL/UNNEST emitted as found-tables), 5 MEDIUM (iceberg `http?://` regex error, sinks.ts SQL-vs-filename disconnect, multi-statement gate tripping on `;` in strings, postinstall hash gap on `alreadyVendored` shortcut, taxonomy worker leak on init timeout), 2 LOW. All 42 fixes shipped in 9 commits + `v1.2.2` tag at `40360b1`. **Headline closes:** CRITICAL XSS in Suggested Reports panel (mounted-file column-name injection → sessionStorage BYOK-key exfil); HIGH SSRF via `?lens=` shared links (now gated behind a confirmation modal listing every host the link would fetch from); HIGH NL→SQL parser triple-bypass (comma-join, single-quoted-FROM replacement-scan, missing INSTALL/LOAD/SET/RESET/USE + multi-statement); HIGH CSP defence-in-depth (`base-uri`, `object-src`, `form-action`, `frame-ancestors`); HIGH supply-chain pin (postinstall sha384-verifies against checked-in `integrity.json`; mismatch → exit 1); HIGH iceberg-catalog strict-https; MEDIUM BYOK-key error-body scrubber + custom-endpoint URL https-only + bearer-token RFC 7235 charset rejection. Plus correctness: `Math.min` stack overflow on ≥125k-row columns, chart-to-SQL binding by `_inputName`, pivot+map cell name editors, compareTables index-based aliases, SW `CACHE_VERSION` injected from script hash, taxonomy worker error/timeout/terminate, `buildSchemaHint` per-table key prefix. **421 vitest (+44 new) / 51/51 e2e / 60 eval / smoke / check** all green; bundle **531.5 KB / 600 KB** (88.6%, 68.5 KB headroom). New files: `src/ui/lens-confirm-modal.ts`, `src/core/bearer-token.ts`, `src/core/sidecar/providers/redact.ts`. Detail: `plan/forward-pass-2026-06-02.md` + `plan/v1.2.2-release-notes.md`. Amendments A19–A23 in `plan/spec-amendments.md`. DECISIONS 2026-06-02 entries cover the load-bearing choices (lens auto-mount UX, hash-pin protocol, two-track adversarial review as standing gate, mountIcebergTable laxer vs catalog strict, frame-ancestors-in-meta).

## Earlier last-update: 2026-05-31T17:00:00Z — **Wave 6 complete (4/4) in a single sweep.** W6.2 presentation mode (`?present=1` hides editor chrome + sidebars, shows only markdown + charts), W6.3 static-HTML export (header button serializes the live notebook DOM into a single self-contained .html with ~3 KB embedded CSS, no JS, no engine), W6.1 interactive-input cell (text / number / date / select widgets whose value inlines into downstream SQL via `@name` ref resolution with quote-doubling SQL-injection safety), W6.4 dashboard layout cell (CSS grid of named markdown / chart / pivot / map cells). Markdown + chart cells gained name inputs so they can be referenced from dashboards. 333 vitest / smoke / check all green. Bundle 517.7 KB / 600 KB (86.3%, 82 KB headroom). **Earlier today** Wave 5 (5/5) + demo verification + SheetJS rawNumbers fix + 3 W4 follow-ups all shipped. Eleven commits today, all on origin/main.

## Earlier last-update: 2026-05-31T16:00:00Z — **W4 #3 raw-events fixture + two taxonomy bugs fixed; Wave 4 now has full evidence.** New `scripts/gen-raw-events-fixture.mjs` deterministically generates a 1500-row JSONL (220 users, 30-day window, Mixpanel/Amplitude/PostHog-shaped) into `public/examples/events/events.jsonl`. Bundle manifest registers it as a third example source ("Product events"). Running it through demo-verify exposed two taxonomy bugs: (1) **`user_id` detector weights wrong for real-world events** — header_match weight 0.6 + distribution weight 0.4 (with `high_cardinality: true`) meant the strong header signal couldn't carry alone when events have many rows per user (low cardinality in sample → 0.2 multiplier → confidence 0.68 → just below the 0.7 resolution bar → "unknown"). Rebalanced to header=0.8, distribution=0.2, dropped the high_cardinality requirement (events data has low-cardinality user_ids by design); confidence now 1.0. (2) **`url` regex required `http(s)://...`** — `page_url` values like `/products` never matched. Extended regex to `^(/[^\s]*|https?://...)$` and added `page_url`/`page_path`/`path`/`endpoint` to header patterns. Result on the new events fixture: **9/9 columns classify (was 7/9), all 6 W4 templates surface as applicable (was 0), DAU instantiation + run-all renders a chart cleanly with no errors.** Wave 4 (W4.1 taxonomy + W4.2 templates + W4.3 funnel + W4.4 cohort + W4.5 paths) now has full end-to-end evidence on real raw event data. 320 vitest / 60 eval / smoke / demo-verify all green; bundle 498.6 KB.

## Earlier last-update: 2026-05-31T15:00:00Z — **SheetJS rawNumbers bug fixed** (third fix surfaced by demo verification). `XLSX.utils.sheet_to_csv` was emitting cells as their formatted display strings (`"830,706"` with thousand separators, `254%` with percent suffix, `01/07/25` via locale) instead of raw numeric values. DuckDB's CSV sniffer typed those columns VARCHAR; the W4.1 amount/percentage/duration detectors missed them; templates didn't surface. Fix: pass `rawNumbers: true` + `dateNF: 'yyyy-mm-dd'` to `sheet_to_csv`. After the fix, the demo xlsx classifies 2 new `amount` columns (was 0); the remaining 77 unknowns are mostly count columns whose headers don't match the W4 detectors (separate calibration follow-up — captured in pending.md). 3 new vitest regression tests proving the negative ("830,706" + "55%" must NOT appear in emitted CSV). 320 vitest / 60 eval / smoke / demo-verify all green; bundle 498.6 KB. **Earlier this session:** Wave 5 complete (W5.1 NL→SQL + W5.2 result-summary + W5.3 quick-charts + W5.4 sensitivity + W5.5 assertion); demo verification keystone ran on the user's xlsx; two additional bugs fixed alongside (COLUMN_PROFILE placeholder removed; runAll skips empty cells).

## Earlier last-update: 2026-05-31T14:00:00Z — **Demo verification keystone ran + two bugs fixed.** `scripts/verify-demo-ecommerce.mjs` (new) mounts the user's `Retention Rate Analysis_Ecommerce.xlsx` via Playwright filechooser (FSA stripped to force input-fallback), captures classification + template fit, instantiates + runs, writes JSON evidence to `plan/demo-verification-ecommerce.json`. Findings: ✅ xlsx mount path healthy (14 sheets, 85 columns, 0 console errors); ✅ `iso_date` detector fired correctly on `start_date`/`end_date`; ⚠️ zero event-shape hits because the xlsx is PRE-AGGREGATED retention metrics, not raw events (real W4 verification still needs a raw-events fixture); 🐛 two fix-on-the-spot bugs surfaced and shipped: (1) COLUMN_PROFILE template emitted a `your_table` placeholder string that errored on Run — now removed from `ALL_TEMPLATES`, panel shows the helpful empty state; (2) `notebook.runAll()` ran the seed empty cell and surfaced noisy "syntax error at end of input" — now skips empty cells, matching every other notebook. Three Wave-5-complete commits earlier today; Wave 5 closed at 5/5. **Earlier today (2026-05-31):** W5.4 sensitivity labels + W5.5 assertion cell + W5.3 schema-panel quick-chart suggestions + W5.2 sidecar Job 6 result-summary cards + W5.1 sidecar Job 5 NL→SQL. Bundle 498.6 KB / 600 KB (83.1%, 101 KB headroom). 317 vitest + 60 eval (across 6 jobs) + headless smoke + demo verification all green. **Next:** Wave 6 (workflow polish — presentation mode, parameters, layout) OR raw-events fixture for full W4 verification.
## Current milestone: **Demoable v1.1 live at https://naklitechie.github.io/NakliData/ — W1.8 GH Pages deploy landed (DECISIONS 2026-05-30).** First demoable hosted build. CDN-load of DuckDB-wasm is the primary path; `?offline=1` uses the 75 MB vendored fallback now also deployed. `.github/workflows/deploy.yml` rebuilds + redeploys on every push to main. **Earlier today (2026-05-30):** W1.6 + W2.6 landed — map cell basemap (opt-in OSM) + deck.gl pairing for >5k points (DECISIONS 2026-05-30; spec amendment A13). W1.6: `settings.mapBasemap: 'none'|'osm'` (default `'none'`), CSP `img-src` carved out for `tile.openstreetmap.org` only (explicit-host, not blanket `https:`), Settings modal section with privacy hint, MapLibre attribution control auto-renders when basemap on. Tiles are images, not scripts — §6 Hard NOT preserved. W2.6: new `src/lazy/deckgl-points.ts` chunk (605 KB) attaches a `ScatterplotLayer` via `@deck.gl/mapbox`'s `MapboxOverlay` IControl when feature count ≥ 5_000. Below the threshold native MapLibre circles still render — zero deck.gl bytes paid. `mountMap()` gains `skipNativePoints` opt + exposes the live `Map`. **Earlier today (2026-05-30):** W3.4b catalog picker; F+G+H+I+K polish slate. **2026-05-29:** Wave 3 main work — W3.1 Job 4, W3.2 slice A seam, W3.3 wire-protocol design, W3.4a bridge client. **Prior session (2026-05-24):** all of Wave 2 + v1.1.0 + Wave 1 close. **Deferred / external-blocked:** W3.2 slice B (real Transformers.js, needs WebGPU); bridge BINARY (separate multi-week OSS repo); W2.1c (Iceberg OAuth2 + SigV4); W3.6 (excel + read_stat, blocked on DuckDB-wasm bump). **Next:** Wave 3 + every stretch item are done on the NakliData-repo side.
## NOTE on dates: this conversation spanned 2026-05-24 → 2026-05-29. Git timestamps are authoritative. Some earlier-written DECISIONS/amendment cross-references carry "2026-05-24" labels from following the in-repo date; the W2.4/W3.1/W3.2A/W3.3/W3.4a commits are git-dated 2026-05-29.
## Build status: green — `dist/index.html` **460 KB** (76.7 % of 600 KB budget; gate enforced by `scripts/check-bundle-size.mjs`); `dist/chunks/codemirror.js` 364 KB lazy; `dist/chunks/observable-plot.js` 276 KB lazy; `dist/chunks/cytoscape-graph.js` 436 KB lazy; `dist/chunks/maplibre-map.js` 1.0 MB lazy; `dist/sw.js` 2.7 KB; vendored extensions ~2.3 MB under `dist/duckdb-extensions/` (not precached); tsc clean (incl. `eval/`); biome 0 errors / 14 warnings; **284 vitest (21 files) + 33 Playwright e2e (21 files)** passing; eval harness dry-run 42/42 (4 jobs); headless smoke green (6 remote-source modals cycle clean).
## Branch state: single `main` branch, pushed to origin. Tags on origin: `v1.0.0` at `5b10b93`, `v1.1.0` at `04feedc`, `v1.2.0` at `cdee341`, `v1.2.1` at `5e095b6`, `v1.2.2` at `40360b1`. Repo path is `~/Code/Apps/NakliData/`.
## Deploy status: **live at https://naklitechie.github.io/NakliData/** (W1.8 — `.github/workflows/deploy.yml` deploys dist/ on every push to main, ~2 min). Repo homepage + description set. W1.4 mirror remains dropped.

## Theme 2 — complete ✅

  1. ✅ Observable Plot lazy chunk → stacked-bar / area-stacked / heatmap (DECISIONS 13:00). Pie + faceted small-multiples deferred.
  2. ✅ Pivot-table cell — new cell kind, in-memory pivot, sum/avg/min/max/count (DECISIONS 17:30).
  3. ✅ Schema-graph modal via Cytoscape.js lazy chunk; taxonomy-type relationships (DECISIONS 18:00).
  4. ✅ Map cell via MapLibre GL JS lazy chunk; no basemap (CSP + privacy clean); deck.gl deferred (DECISIONS 18:30).
  5. ✅ DuckDB spatial extension via `ST_Read` → `.geojson` / `.kml` mounts (Shapefile deferred — needs multi-file FSA picker).

Next push, in suggested order (from `plan/checkpoint-2026-05-17.md`):
  - **Theme 1 wave 3** — sample-data regen + vendor DuckDB extensions for offline-grade smoke.
  - **Theme 4** — schema + data quality polish (column-statistics panel, side-by-side data compare, type-override learns, demo/censor mode).
  - **AI sidecar** — v1.1 spec §4.3 + portfolio mandate. Three jobs (explain-this-query / explain-this-error / recommend-a-template) + BYOK plumbing.

After Theme 2:
  - **Theme 1 wave 3** — sample-data regen + vendor DuckDB extensions for offline smoke.
  - **Theme 4** — schema + data quality polish (column-statistics panel, side-by-side data compare, type-override learns, demo/censor mode).

## Theme 3 wave 2 — complete (earlier today)

  1. ✅ URL-state sharing (`?lens=<base64>`) — shipped.
  2. ✅ PWA installability — shipped (lite cache, not full; DECISIONS 11:50).
  3. ✅ Multi-session sidebar — shipped as a header dropdown (DECISIONS 12:10).

## Session highlights — 2026-05-17 (Theme 2 wave 4: map cell + GeoJSON/KML mount)

- **New cell kind** `'map'` alongside SQL / chart / markdown / pivot. `MapCellState` in `src/ui/cells/types.ts` with input + geometry + optional color-by pickers. `renderMapCell` in `src/ui/cells/map-cell.ts` (~140 lines) handles both string- and object-shaped GeoJSON values; lazy-loads MapLibre on first render.
- **`src/lazy/maplibre-map.ts`** (~170 lines) renders a FeatureCollection on a tile-less MapLibre canvas (no basemap — CSP/privacy clean). Three layers: polygons (+ outline), lines, points. Optional categorical color via `match` expression. Auto-fit bounds. MapLibre CSS skipped (only matters for popups/controls we don't use).
- **`src/core/engine.ts`** new `registerSpatial` uses `ensureExtension('spatial')` then `ST_Read(...)` with `ST_AsGeoJSON(geom) AS geometry, * EXCLUDE (geom)` so the JS side gets a clean GeoJSON-string column.
- **Mount layer**: `'geojson' | 'kml'` in `FileFormat`; `detectFormat` recognises `.geojson`/`.geo.json`/`.kml` (case-insensitive); file-picker accept list extended. Spec §3.1 supported formats: 13 → 15.
- 3 new vitest specs (format detection) + 2 new Playwright e2e specs (literal-GeoJSON SQL → map renders; non-GeoJSON → friendly fallback).

## Session highlights — 2026-05-17 (Theme 2 wave 3: schema-graph modal)

- **`src/lazy/cytoscape-graph.ts`** (new, ~100 lines) wraps Cytoscape with a Rangrez-palette stylesheet + cose layout. Lazy-loaded; never touches the shell.
- **`src/ui/schema-graph.ts`** (new, ~100 lines) owns the modal: open/close, fetch taxonomy bundle (reuses `getTaxonomyClient().ensureReady`), filter types to those participating in relationships, lazy-load Cytoscape, render. Backdrop click, close icon, and Escape all dismiss.
- **Taxonomy bundle** now optionally carries `relationships: TypeRelationship[]`. `src/taxonomy/load.ts` fetches `relationships.json` best-effort (failure doesn't break the classifier path).
- **Schema panel header** gets a small chart-icon button → `data-action="open-schema-graph"`. Discoverable even before sources are mounted.
- 2 new Playwright specs (`tests/e2e/schema-graph.spec.ts`): button → overlay → chunk fetched → `<canvas>` appears → Escape closes; backdrop closes; close-icon closes.

## Session highlights — 2026-05-17 (Theme 2 wave 2: pivot-table cell)

- **New cell kind** `'pivot'` alongside SQL / chart / markdown. `PivotCellState` in `src/ui/cells/types.ts` with input/row/col/value/agg pickers. `renderPivotCell` in `src/ui/cells/pivot-cell.ts` (~290 lines).
- **In-memory pivot** over the upstream SQL cell's `lastResult.rows` — same pattern as chart-cell. No extra DuckDB query. `computePivot` is exported pure-function for unit testing.
- **Aggregations**: sum / avg / min / max / count. `count` works without a value column. Row + column + grand totals shown only when totals are semantically meaningful (sum, count) — `hasMeaningfulTotals` flag gates the `<tfoot>` render.
- **Display cap** 200 rows × 50 cols with a "more hidden" footnote. BIGINT + numeric-string coercion; non-numeric values silently dropped for sum/avg/min/max.
- **Notebook** gets a "+ Pivot" toolbar button. `addCell('pivot')` seeds defaults.
- 7 new vitest specs (pure-function pivot logic) + 1 new Playwright e2e (full UI flow: mount → SQL → run → add pivot → pick pickers → assert numeric cells + grand-total tfoot).

## Session highlights — 2026-05-17 (Theme 2 wave 1: Observable Plot lazy chunk)

- New `src/lazy/observable-plot.ts` (~130 lines) dispatches `stacked-bar` / `area-stacked` / `heatmap` to Plot marks. Reuses the existing lazy-loading infra from Theme 1 wave 2 — Plot lives at `dist/chunks/observable-plot.js` (273 KB lazy) and never touches the shell.
- `src/charts/render.ts` gets a `PLOT_TYPES` set + fire-and-forget `loadChunk('observable-plot')` for the new types. Existing 7 hand-rolled chart types unchanged.
- ChartCellState union + chart-cell picker extended with the three new types.
- Heuristics in `mountPlotChart`: `pickCategory` finds a categorical column for the fill channel; BIGINT-from-DuckDB on the y channel coerced to Number so Plot's stack math doesn't choke.
- 2 new Playwright specs in `tests/e2e/plot-chart-types.spec.ts` — chunk-load assertion via `page.on('request')` + SVG-with-marks check after switching chart type; heatmap on bad data falls back without throwing.
- **Single `main` branch now**: the bootstrap `claude/agent-handoff-start-3c2Ib` branch was deleted local + remote after the v1.0 tag pushed.

## Session highlights — 2026-05-17 (Theme 3 wave 2, item 3: multi-session sidebar)

- **`src/core/sessions.ts`** (new) owns the multi-session storage. CRUD over `sessions/index` + per-session snapshot at `sessions/<id>/snapshot`. First-boot migration adopts the legacy `workbook/current` value as the seed session and deletes the old key.
- **`src/main.ts`** boot ends with `ensureActiveSession()` → `refreshSessionSwitcher()` → restore (either `decodeLensParam` or active-session snapshot). New `switchToSession(engine, root, id)` flushes-then-flips so in-flight debounced saves land on the outgoing session. New handlers: `session-menu` / `session-new` / `session-switch` / `session-rename` / `session-delete`. Outside-click closes the dropdown.
- **`src/ui/shell.ts`** + **`src/ui/shell.css.ts`** add the header switcher: trigger chip (active session name + caret) → popup with "New session" + per-session row (switch / rename / delete).
- **`src/core/persistence.ts`** trimmed: removed `saveWorkbookSnapshot` / `loadWorkbookSnapshot` / `clearWorkbookSnapshot` (sessions.ts replaces them). `.naklidata` file save/load surface untouched.
- 13 new vitest specs in `tests/sessions.test.ts` (in-memory IDB shim via `vi.mock`; CRUD + migration + snapshot round-trip). 2 new Playwright specs in `tests/e2e/sessions.spec.ts` (full UI flow + last-session-deletion guard).

## Session highlights — 2026-05-17 (Theme 3 wave 2, item 2: PWA installability)

- **`public/manifest.webmanifest`** declares `name`, `start_url: ./`, `display: standalone`, theme/background colors, single icon with `any maskable` purpose.
- **`public/icon.svg`** — 256×256 brand-mark on accent background, 20% inset for the maskable safe area.
- **`public/sw.js`** (~85 lines, vanilla): precache shell + chunks + manifest + icon + taxonomy worker on `install`; cleanup stale caches on `activate`; SWR for same-origin GETs at runtime; cross-origin pass-through; navigation requests offline → cached `index.html`. DuckDB-fallback bytes (~74 MB) NOT precached — opportunistically cached if the user boots with `?offline=1` once. See DECISIONS 11:50 for why lite-not-full.
- `src/index.html` adds the manifest link, theme-color, application-name. `src/main.ts` registers the SW at window `load` only when `process.env.NODE_ENV === 'production'` (esbuild replaces at build time; DEV skips registration to avoid stale-asset surprises during watch).
- `.webmanifest` (→ `application/manifest+json`) + `.svg` MIME mappings added to both `scripts/smoke.mjs` and the e2e fixture server.
- 2 new Playwright specs in `tests/e2e/pwa.spec.ts` (manifest fetch + parse + maskable icon; SW registers + precaches + serves cached shell when `context.setOffline(true)` + reload).

## Session highlights — 2026-05-17 (Theme 3 wave 2, item 1: URL-state sharing)

- **New `src/core/url-state.ts`** — `encodeLensParam` / `decodeLensParam` via browser-native `CompressionStream('gzip')` + base64url. `buildShareUrl()`, `readLensFromLocation()`, `clearLensFromLocation()` complete the surface. No new dependencies.
- **Boot precedence**: `?lens=` overrides the IDB workbook snapshot. On bad lens, fall back to IDB (not empty state) so a malformed link doesn't wipe the user's work. URL is stripped via `replaceState` after applying — refresh doesn't re-trigger.
- **Share button** in the shell header (next to Save). Action `share-link` serializes the workbook, encodes, copies to clipboard. Soft warning when URL > 7.8 KB (some chat tools truncate).
- 4 new vitest specs in `tests/url-state.test.ts` (round-trip, compression ratio, malformed-base64, non-`.naklidata` payload). 2 new Playwright specs in `tests/e2e/url-state-share.spec.ts` (Share-and-load round-trip with clipboard.writeText stubbed for headless determinism; corrupted-lens fallback).
- `tests/e2e/playwright.config.ts`: env-var aligned with `scripts/smoke.mjs` convention (`PLAYWRIGHT_CHROMIUM_PATH`, fallback to legacy `CHROMIUM_PATH`, fallback to Playwright bundled chromium). Capped at `workers: 2` — default `workers: N-cores` caused intermittent engine-boot timeouts from parallel DuckDB-wasm boots.

## Session highlights — 2026-05-17 (desktop pickup, tag landed)

- **v1.0.0 tag pushed** — annotated tag at `5b10b93` per `plan/v1.0-handoff-notes.md`. Web session created the tag locally but couldn't push (sandbox 403 on tag pushes); desktop session landed it cleanly.
- **GitHub default branch switched to `main`** — was the bootstrap leftover `claude/agent-handoff-start-3c2Ib`. Both branches stay tracked.
- **Smoke script made portable** — `scripts/smoke.mjs` no longer hardcodes the sandbox chromium path. Uses `PLAYWRIGHT_CHROMIUM_PATH` env var if set; otherwise lets Playwright pick the bundled chromium. DECISIONS entry at 2026-05-17 11:10.
- Desktop-handoff checklist passed end-to-end: install (postinstall vendored DuckDB + integrity.json), check (clean, 14 expected biome warnings), test (60/60), smoke (all 12 assertions; mounted 4 source tables = sandbox's 3 + the JSONL log that needs `extensions.duckdb.org`), build size under budget.

## Session highlights — 2026-05-17 (pre-tag bundle)

- **CodeMirror 6 returns as a lazy chunk.** `src/lazy/codemirror.ts` exports `mountSqlEditor(host, opts)` (SQL syntax + autocomplete + line numbers). `src/ui/cells/sql-cell.ts` renders a textarea first and asynchronously upgrades to CM6 once the chunk lands; per-cell-id `cmInstances` map preserves editor state across notebook re-renders. `disposeSqlCellEditor(cellId)` released on cell delete. Shell stays under the 600 KB gate (320 KB); CM6 chunk is 370 KB, fetched only when a SQL cell renders. Closes the §7.1 vs §1 spec tension recorded in DECISIONS 2026-05-15 14:10.
- **DuckDB-wasm SRI pinning.** `scripts/fetch-duckdb-fallback.mjs` now sources from `node_modules/@duckdb/duckdb-wasm/dist/` first (with CDN fallback) and writes `public/duckdb-fallback/integrity.json` with SHA-384 hashes per file. `src/core/engine.ts` imports the integrity manifest and, on the CDN path (`!opts.offline`), calls `fetchWithSri(url, integrity)` for the worker JS + wasm before passing them to DuckDB as blob URLs. Closes the §7.1 gate artifact "DuckDB-wasm boots from CDN with SRI."
- **README pass per spec §3.10.** Full rewrite: what it is, what it isn't, browser support (Chrome/Edge/Opera 122+ / Firefox partial / Safari unsupported), quick start (end-user + dev), example data, `.naklidata` format, taxonomy contribution flow, privacy posture (SRI verification + workspace IDB persistence + BYOK sessionStorage default with opt-in IDB), license, links to STATUS / DECISIONS / plan / CLAUDE.md.

## Session highlights — earlier 2026-05-17

- Theme 1 wave 2: lazy code-splitting infra + Apache Arrow IPC mount. 5 new tests; spec §3.1 formats list at 13.
- Theme 3 wave 1: workspace state auto-saves + auto-restores from IDB (per spec amendment A1). 2 new e2e tests.

## Sandbox limitation
Dev sandbox blocks `extensions.duckdb.org` so Theme 1 mounts requiring DuckDB extensions can't be exercised in the local smoke. User's browser works fine; vendoring extensions (Theme 1 wave 3) closes the local gap.
