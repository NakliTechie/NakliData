node_modules/
dist/
.cache/
.DS_Store
*.log
.env
.env.*
!.env.example
coverage/
.vitest-cache/
public/duckdb-fallback/duckdb-*.wasm
public/duckdb-fallback/duckdb-browser-*.worker.js
# DuckDB extension wasm bytes are fetched by postinstall
# (scripts/fetch-duckdb-extensions.mjs). integrity.json is committed
# as the pinned-version record; the binaries themselves aren't.
public/duckdb-extensions/**/*.duckdb_extension.wasm
test-results/
# Eval harness: generated report + esbuild bundle cache (eval/.cache/ is
# also covered by the .cache/ rule above, listed here for clarity).
eval/report.html
eval/.cache/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example

# plan/ working notes (gitignored 2026-06-23 — /replan-nt). NOTE: a few plan/
# files are canonical project records, not working notes — spec-amendments.md
# (the ratified A1–A33 log, referenced by CLAUDE.md) and the v*-release-notes.md.
# See plan/pending.md "Open questions": consider moving those out of plan/ to
# keep them tracked.
plan/
